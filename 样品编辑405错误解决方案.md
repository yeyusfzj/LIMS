# 样品编辑 405 错误解决方案

## 问题

用户在样品详情页面编辑样品后保存时，收到 **405 Method Not Allowed** 错误。

## 原因

前端使用 `PUT` 方法更新样品，但 FastAPI 后端只支持 `PATCH` 方法。

## 解决方案

修改前端代码，将 HTTP 方法从 `PUT` 改为 `PATCH`。

### 修改文件

**文件**: `vue-project/src/services/api/sample.ts`

**第 169 行**，将：
```typescript
const response = await http.put(`${this.baseUrl}/${id}`, updateData)
```

改为：
```typescript
const response = await http.patch(`${this.baseUrl}/${id}`, updateData)
```

## 验证

运行测试脚本验证修复：

```bash
node test-sample-update.js
```

测试结果：
- ✓ PUT 方法返回 405 错误（符合预期）
- ✓ PATCH 方法更新成功

## 相关文档

- 详细 bugfix 文档：`.kiro/specs/sample-edit-405-fix/bugfix.md`
- API 一致性规则：`.kiro/steering/api-consistency.md`
- 后端选择规则：`.kiro/steering/backend-selection.md`

## 修复日期

2026-04-23
