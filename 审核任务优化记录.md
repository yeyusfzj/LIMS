# 审核任务优化记录

## 优化时间
2026-05-04

## 优化内容

### 1. 删除非实验室相关任务
- **问题**：客户投诉处理任务不属于实验室相关任务
- **解决**：从样例数据脚本中删除客户投诉任务类型
- **影响文件**：`backend-api/add-diverse-audit-tasks.js`

### 2. 修复任务详情显示问题
- **问题**：校准、生产、评审等任务类型的数据显示为原始JSON代码
- **解决**：为每种任务类型添加专门的显示模板
- **影响文件**：`vue-project/src/views/audit/AuditTaskDetail.vue`

## 新增任务类型显示模板

### 校准任务（CALIBRATION）
显示内容：
- 设备信息（设备名称、设备编号）
- 校准信息（校准日期、下次校准日期、校准人员）
- 校准结果（合格/不合格、精度、证书编号）
- 校准测试点表格（标准值、测量值、偏差、结果）

### 生产任务（PRODUCTION）
显示内容：
- 生产信息（批次号、产品名称、生产数量）
- 质量状态（合格/待检验）
- 时间信息（开始时间、预计完成时间）
- 当前进度（进度条）
- 工艺优化项表格（优化项目、优化前、优化后、改进效果）

### 评审任务（REVIEW）
显示内容：
- 文档信息（文档标题、文档类型、版本号）
- 作者信息（作者、提交日期、页数）
- 主要变更内容（列表形式）
- 评审人员表格（评审人、评审状态）

### 审批任务（APPROVAL）- 增强
新增显示：
- 审批金额和币种
- 审批原因
- 审批项目清单表格（项目名称、规格型号、数量、单价）

## 当前支持的任务类型

1. **检测任务（TEST/TASK）**
   - 显示检测结果表格
   - 包含检测参数、结果、方法、数据来源等

2. **研发任务（RESEARCH/RD）**
   - 显示产品信息
   - 包含产品名称、编号、类型、研发阶段等

3. **审批任务（APPROVAL）**
   - 显示审批信息和项目清单
   - 包含审批类型、申请人、金额、项目明细等

4. **校准任务（CALIBRATION）** ✨ 新增
   - 显示校准信息和测试点
   - 包含设备信息、校准结果、测试数据等

5. **生产任务（PRODUCTION）** ✨ 新增
   - 显示生产信息和工艺优化
   - 包含批次信息、进度、优化项等

6. **评审任务（REVIEW）** ✨ 新增
   - 显示文档信息和评审状态
   - 包含文档详情、变更内容、评审人员等

## 样例数据

运行脚本后创建的审核任务：

1. **新产品配方研发** (RESEARCH) - 样品审核 - 待审核
2. **设备采购审批** (APPROVAL) - 技术审核 - 待审核
3. **生产工艺优化** (PRODUCTION) - 分析审核 - 审核中
4. **质量体系文件评审** (REVIEW) - 质量审核 - 待审核
5. **仪器设备校准** (CALIBRATION) - 分析审核 - 已通过

## 测试方法

1. 访问审核任务列表：http://localhost:5173/audit/tasks
2. 点击不同类型的审核任务查看详情
3. 验证每种任务类型的数据显示是否正确
4. 特别关注校准任务的测试点表格显示

## 技术实现

### 判断函数
```typescript
// 校准任务判断
const isCalibrationTask = () => {
  const nodeType = auditTask.value.task?.nodeType?.toUpperCase() || ''
  const nodeName = auditTask.value.task?.nodeName?.toLowerCase() || ''
  return nodeType.includes('CALIBRATION') || nodeName.includes('校准') || nodeName.includes('校验')
}

// 生产任务判断
const isProductionTask = () => {
  const nodeType = auditTask.value.task?.nodeType?.toUpperCase() || ''
  const nodeName = auditTask.value.task?.nodeName?.toLowerCase() || ''
  return nodeType.includes('PRODUCTION') || nodeName.includes('生产') || nodeName.includes('制造')
}

// 评审任务判断
const isReviewTask = () => {
  const nodeType = auditTask.value.task?.nodeType?.toUpperCase() || ''
  const nodeName = auditTask.value.task?.nodeName?.toLowerCase() || ''
  return nodeType.includes('REVIEW') || nodeName.includes('评审') || nodeName.includes('审查')
}
```

### 动态标题
```typescript
const getTaskDataTitle = () => {
  if (isTestTask()) return '检测数据'
  if (isResearchTask()) return '产品信息'
  if (isApprovalTask()) return '审批信息'
  if (isCalibrationTask()) return '校准信息'
  if (isProductionTask()) return '生产信息'
  if (isReviewTask()) return '评审信息'
  return '任务相关数据'
}
```

## 优化效果

✅ 删除了非实验室相关的客户投诉任务
✅ 校准任务数据以结构化表格形式显示，不再是原始JSON
✅ 生产任务显示进度条和工艺优化表格
✅ 评审任务显示文档变更内容和评审人员状态
✅ 审批任务增加了金额和项目清单显示
✅ 所有实验室相关任务类型都有专门的显示模板

## 后续建议

1. 可以根据实际业务需求添加更多任务类型
2. 可以为每种任务类型添加更详细的数据验证
3. 可以添加任务数据的导出功能
4. 可以添加任务数据的打印功能
