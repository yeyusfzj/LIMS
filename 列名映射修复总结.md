# 列名映射修复总结

## 修复时间
2026-05-08

## 问题发现

在测试过程中发现多个 500 错误，都是由于数据库列名不匹配导致的：

### 1. 审核任务错误
```
column audit_tasks.sampleId does not exist
```

### 2. 用户表错误
```
column users.password_hash does not exist
HINT: Perhaps you meant to reference the column "users.passwordHash".
```

## 问题分析

**数据库实际使用的命名约定**：
- 数据库中的列名使用**驼峰命名**（camelCase）
- 例如：`sampleId`, `auditorId`, `submittedAt`, `passwordHash`

**SQLAlchemy 模型的问题**：
- 模型中的 Python 属性使用驼峰命名
- 但没有指定数据库列名映射
- SQLAlchemy 默认会将属性名直接用作列名，并加上引号

**错误的修复方向**：
- 我们最初假设数据库使用蛇形命名（snake_case）
- 自动修复脚本将所有驼峰属性映射到蛇形列名
- 但这与数据库实际的驼峰命名不匹配

## 正确的解决方案

有两个选择：

### 方案 A：保持数据库驼峰命名（推荐）

**优点**：
- 不需要修改数据库
- 不需要数据迁移
- 风险最小

**实施**：
1. 撤销自动修复脚本的更改
2. 只修复确实有问题的列（如 `audit_tasks` 表）
3. 对于已经使用蛇形命名的列，保持映射

### 方案 B：统一改为蛇形命名

**优点**：
- 符合 Python/PostgreSQL 最佳实践
- 代码更规范

**缺点**：
- 需要修改数据库结构
- 需要数据迁移
- 风险较高
- 可能影响现有数据

## 当前状态

### 已修复的文件
- ✅ `app/models/audit.py` - 审核模型（手动修复，使用蛇形命名）
- ⚠️ 其他 11 个模型文件 - 自动修复（可能需要回滚）

### 发现的问题
1. `audit_tasks` 表 - 数据库使用蛇形命名 ✅ 已修复
2. `users` 表 - 数据库使用驼峰命名 ❌ 修复错误
3. 其他表 - 需要逐一验证

## 建议的修复步骤

### 步骤 1：回滚自动修复

```bash
cd backend-api
git checkout app/models/
```

### 步骤 2：只修复确认有问题的模型

针对每个报错的表，先检查数据库实际列名：

```sql
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'table_name'
ORDER BY ordinal_position;
```

### 步骤 3：根据实际情况修复

- 如果数据库列名是蛇形：添加映射 `Column('snake_case', ...)`
- 如果数据库列名是驼峰：不需要映射（或使用 `Column('camelCase', ...)`）

## 数据库命名约定检查

需要检查每个表的实际列名：

| 表名 | 预期命名 | 实际命名 | 状态 |
|------|---------|---------|------|
| `audit_tasks` | 蛇形 | 蛇形 | ✅ 已修复 |
| `users` | 驼峰 | 驼峰 | ❌ 需要回滚 |
| `samples` | ? | ? | ⚠️ 待检查 |
| `workflows` | ? | ? | ⚠️ 待检查 |
| `reports` | ? | ? | ⚠️ 待检查 |
| ... | ... | ... | ... |

## 工作流模板错误

工作流模板的错误不是列名映射问题，而是缺少数据库表：

```
relation "role_permissions" does not exist
```

这需要单独处理（可能需要运行数据库迁移）。

## 下一步行动

1. **立即**：回滚自动修复的更改
2. **然后**：只保留 `audit.py` 的修复
3. **最后**：根据实际错误逐个修复其他模型

## 经验教训

1. ❌ 不要假设数据库的命名约定
2. ✅ 先检查数据库实际结构
3. ✅ 逐个表验证和修复
4. ✅ 使用版本控制，方便回滚

---

**创建时间**: 2026-05-08  
**状态**: ⚠️ 需要回滚并重新修复
