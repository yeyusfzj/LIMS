# 目录重命名说明

## 变更日期
2026-05-07

## 变更原因
为了让 AI 助手能够第一时间识别并启动正确的后端服务，我们对项目目录进行了重命名优化。

## 变更内容

### 1. 目录重命名

| 原目录名 | 新目录名 | 说明 |
|---------|---------|------|
| `fastapi-backend/` | `backend-api/` | FastAPI 后端（当前使用的主后端） |
| `backend-api/` | `backend-api-deprecated/` | Node.js 后端（已弃用） |

### 2. 启动脚本

创建了两个启动脚本，按正确顺序启动前后端服务：

- **Windows**: `start-dev.bat`
- **Linux/Mac**: `start-dev.sh`

#### 启动脚本功能
1. 先启动 FastAPI 后端（端口 8001）
2. 等待 3 秒
3. 再启动 Vue 前端（端口 5173）
4. 显示访问地址

#### 使用方法

**Windows:**
```bash
start-dev.bat
```

**Linux/Mac:**
```bash
chmod +x start-dev.sh
./start-dev.sh
```

## 优势

### 1. 目录重命名的优势
- ✅ **字母顺序优先**: `backend-api` 在字母顺序上排在 `backend-api-deprecated` 前面
- ✅ **语义清晰**: 主后端使用简洁名称 `backend-api`，弃用后端明确标注 `deprecated`
- ✅ **AI 识别**: AI 助手会优先选择 `backend-api` 而不是 `backend-api-deprecated`
- ✅ **符合最佳实践**: 当前使用的服务用简洁名称，历史版本用描述性名称

### 2. 启动脚本的优势
- ✅ **一键启动**: 无需手动启动多个服务
- ✅ **正确顺序**: 确保后端先启动，前端后启动
- ✅ **跨平台**: 提供 Windows 和 Linux/Mac 两个版本
- ✅ **清晰提示**: 显示启动进度和访问地址

## 影响范围

### 需要更新的配置文件
- ✅ `vue-project/BACKEND_CONFIGURATION.md` - 已更新
- ✅ `vue-project/.env.development` - 无需更新（端口未变）
- ✅ `vue-project/src/services/http.ts` - 无需更新（端口未变）

### 不受影响的内容
- ✅ 前端配置（`.env.development`）- 端口仍为 8001
- ✅ API 端点 - 路径未变
- ✅ 数据库连接 - 配置未变
- ✅ 环境变量 - 配置未变

## 验证步骤

### 1. 验证目录结构
```bash
ls -la | grep backend
```

应该看到：
```
backend-api/              # FastAPI 后端（主后端）
backend-api-deprecated/   # Node.js 后端（已弃用）
```

### 2. 验证启动脚本
```bash
# Windows
start-dev.bat

# Linux/Mac
./start-dev.sh
```

### 3. 验证服务运行
- 前端: http://localhost:5173/
- 后端: http://localhost:8001/
- API 文档: http://localhost:8001/docs

## 回滚方案

如果需要回滚到原来的目录结构：

```bash
# 停止所有服务
# 然后执行：

# 重命名回原来的名称
mv backend-api fastapi-backend
mv backend-api-deprecated backend-api
```

## 相关文档

- [后端配置说明](vue-project/BACKEND_CONFIGURATION.md)
- [FastAPI 后端设计](FastAPI后端设计.md)
- [前端设计](前端设计.md)

## 注意事项

1. **旧后端已弃用**: `backend-api-deprecated/` 目录保留仅供参考，不应再使用
2. **启动脚本优先**: 建议使用启动脚本而不是手动启动
3. **端口未变**: FastAPI 后端仍使用端口 8001，前端仍使用端口 5173
4. **数据库位置**: 数据库文件位于 `backend-api/laboratory.db`

## 常见问题

### Q: 为什么要重命名目录？
**A**: 为了让 AI 助手能够第一时间识别并启动正确的后端服务。原来的命名方式（`backend-api` 和 `fastapi-backend`）在字母顺序上会让 AI 优先选择旧的 Node.js 后端。

### Q: 旧的 backend-api 目录还能用吗？
**A**: 不建议使用。它已被重命名为 `backend-api-deprecated` 并标记为已弃用。所有新功能都应该在新的 `backend-api`（原 `fastapi-backend`）中开发。

### Q: 启动脚本会自动安装依赖吗？
**A**: 不会。启动脚本假设你已经安装了所有依赖。如果是首次运行，请先：
```bash
# 安装后端依赖
cd backend-api
pip install -r requirements.txt

# 安装前端依赖
cd vue-project
npm install
```

### Q: 可以只启动前端或后端吗？
**A**: 可以。启动脚本会在两个独立的终端窗口中运行服务，你可以单独关闭其中一个。或者手动启动：
```bash
# 只启动后端
cd backend-api
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001

# 只启动前端
cd vue-project
npm run dev
```

---

**最后更新**: 2026-05-07  
**维护者**: 开发团队  
**状态**: ✅ 已完成
