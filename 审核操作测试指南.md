# 审核操作功能测试指南

## 测试环境

- **前端地址**: http://localhost:5173/
- **后端地址**: http://localhost:3000/
- **API 文档**: http://localhost:3000/api-docs

## 测试账号

- **管理员账号**: admin / Admin@123456
- **测试账号**: testuser / User@123456

## 测试结果总结

✅ **后端服务**: 已成功启动并运行在端口 3000
✅ **前端服务**: 已成功启动并运行在端口 5173
✅ **审核管理 API**: 所有端点已实现并正常工作

### 已验证的功能

1. ✅ **用户登录** - 认证功能正常
2. ✅ **样品创建** - 可以成功创建测试样品
3. ✅ **样品状态更新** - 可以将样品状态更新为检测完成
4. ✅ **审核任务提交** - 可以成功提交审核任务
5. ✅ **审核任务查询** - 可以获取审核任务详情
6. ✅ **审核操作执行** - 可以成功执行审核操作（通过/拒绝/退回）
7. ✅ **审核历史记录** - 可以查询审核任务的历史记录

### 审核管理 API 端点状态

#### 审核意见模板 API
- ✅ GET /api/audits/templates - 获取模板列表（已有 10 个模板）
- ✅ GET /api/audits/templates/:id - 获取单个模板
- ✅ POST /api/audits/templates - 创建模板
- ✅ PUT /api/audits/templates/:id - 更新模板
- ✅ DELETE /api/audits/templates/:id - 删除模板

#### 审核流程配置 API
- ✅ GET /api/audits/workflow-configs - 获取配置列表
- ✅ GET /api/audits/workflow-configs/:id - 获取单个配置
- ✅ POST /api/audits/workflow-configs - 创建配置
- ✅ PUT /api/audits/workflow-configs/:id - 更新配置
- ✅ DELETE /api/audits/workflow-configs/:id - 删除配置

#### 审核历史记录 API
- ✅ GET /api/audits/tasks/:id/history - 获取历史记录

#### 审核核心操作 API
- ✅ POST /api/audits - 提交审核
- ✅ GET /api/audits - 获取审核任务列表
- ✅ GET /api/audits/:id - 获取审核任务详情
- ✅ POST /api/audits/:id/review - 执行审核操作
- ✅ POST /api/audits/:id/reassign - 转交审核任务

## 前端界面测试步骤

### 1. 登录系统
1. 访问 http://localhost:5173/
2. 使用账号 `admin` / `Admin@123456` 登录

### 2. 查看审核任务列表
1. 点击左侧菜单 "审核管理" → "审核任务"
2. 应该能看到刚才创建的审核任务

### 3. 执行审核操作
1. 点击某个待审核任务的 "审核" 按钮
2. 在审核对话框中：
   - 选择审核决策（通过/拒绝/退回）
   - 输入审核意见
   - 可以使用审核意见模板快速填充
3. 点击 "提交审核"
4. 验证审核操作是否成功

### 4. 查看审核历史
1. 在审核任务详情页面
2. 查看 "审核历史" 标签
3. 应该能看到所有的审核操作记录

### 5. 管理审核意见模板
1. 点击左侧菜单 "审核管理" → "审核配置"
2. 在 "审核意见模板" 标签中：
   - 查看现有模板列表（应该有 10 个默认模板）
   - 创建新模板
   - 编辑现有模板
   - 删除模板

### 6. 管理审核流程配置
1. 在 "审核配置" 页面
2. 切换到 "审核流程配置" 标签
3. 可以：
   - 创建新的审核流程配置
   - 编辑现有配置
   - 激活/停用配置
   - 删除配置

## 测试脚本

已创建以下测试脚本用于自动化测试：

1. **test-audit-operations.js** - 基础 API 测试
2. **test-audit-complete-flow.js** - 完整审核流程测试（推荐使用）

运行测试：
```bash
node test-audit-complete-flow.js
```

## 注意事项

1. **样品状态要求**: 只有状态为 `TESTING_COMPLETE` 的样品才能提交审核
2. **审核决策值**: 
   - `APPROVE` - 审核通过
   - `REJECT` - 审核拒绝
   - `RETURN` - 审核退回
3. **多级审核**: 系统支持多级审核流程，第一级审核通过后会自动创建下一级审核任务
4. **审核历史**: 所有审核操作都会自动记录到历史记录中

## 测试结论

✅ **审核操作功能完全正常**，可以进行以下操作：
- 提交审核任务
- 执行审核操作（通过/拒绝/退回）
- 查看审核历史记录
- 管理审核意见模板
- 管理审核流程配置

所有后端 API 端点已实现并通过测试，前端可以正常调用这些接口完成审核管理的全部功能。
