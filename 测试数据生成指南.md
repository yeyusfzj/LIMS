# 测试数据生成指南

## 📋 概述

为了测试系统的分页功能，我们需要生成大量的测试数据。本指南提供了多种方法来生成测试数据。

## 🎯 目标数据量

| 数据类型 | 建议数量 | 用途 |
|---------|---------|------|
| 样品 | 150+ | 测试样品列表分页 |
| 仪器 | 80+ | 测试仪器列表分页 |
| 工作流 | 50+ | 测试工作流列表分页 |
| 审核任务 | 120+ | 测试审核任务列表分页 |

## 🚀 方法一：使用Python脚本（推荐）

### 1. 准备工作

确保后端服务已启动：
```bash
cd backend-api
.\venv\Scripts\activate  # Windows
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 运行数据生成脚本

我已经创建了以下脚本：

#### 完整版（推荐）
```bash
cd backend-api
python seed_large_test_data.py
```

生成内容：
- 150个样品
- 80台仪器
- 50个工作流
- 120个审核任务

#### 简化版（仅样品和仪器）
```bash
cd backend-api
python seed_samples_instruments.py
```

生成内容：
- 150个样品
- 80台仪器

#### 原始版（少量数据）
```bash
cd backend-api
python seed_test_data.py
```

生成内容：
- 30个样品
- 29个审核任务

### 3. 验证数据

访问前端页面查看数据：
- 样品列表: http://localhost:5173/samples
- 仪器列表: http://localhost:5173/instruments
- 工作流列表: http://localhost:5173/workflows
- 审核任务列表: http://localhost:5173/audit

## 🔧 方法二：使用API手动创建

如果Python脚本无法运行，可以使用API手动创建数据。

### 1. 创建样品

```bash
# 使用curl或Postman发送请求
POST http://localhost:8000/api/v1/samples
Content-Type: application/json
Authorization: Bearer <your_token>

{
  "barcode": "S20260512001",
  "sample_name": "水质样品-1",
  "sample_type": "水质样品",
  "client_name": "华泰检测有限公司",
  "sampling_date": "2026-05-12T10:00:00",
  "received_date": "2026-05-12T14:00:00",
  "quantity": 500,
  "unit": "ml",
  "status": "registered",
  "current_location": "仓库-A-1",
  "priority": "normal",
  "description": "样品外观正常"
}
```

### 2. 创建仪器

```bash
POST http://localhost:8000/api/v1/instruments
Content-Type: application/json
Authorization: Bearer <your_token>

{
  "code": "INS20260001",
  "name": "安捷伦 气相色谱仪",
  "model": "Agilent-7890",
  "manufacturer": "安捷伦",
  "serial_number": "SN123456",
  "purchase_date": "2020-01-01",
  "status": "IN_USE",
  "location": "实验室-A-1号位",
  "responsible_person": "admin",
  "next_calibration_date": "2026-12-31",
  "specifications": "精度: ±0.001, 量程: 1000",
  "notes": "状态良好"
}
```

## 📊 方法三：使用数据库直接插入

如果需要快速生成大量数据，可以直接在数据库中执行SQL语句。

### 1. 连接数据库

```bash
# PostgreSQL
psql -U postgres -d laboratory_db

# MySQL
mysql -u root -p laboratory_db
```

### 2. 批量插入样品

```sql
-- 生成100个样品
INSERT INTO samples (
  id, barcode, sample_name, sample_type, client_name,
  sampling_date, received_date, quantity, unit, status,
  current_location, priority, description, created_by, created_at
)
SELECT
  gen_random_uuid(),
  'S' || to_char(CURRENT_DATE, 'YYYYMMDD') || lpad(generate_series::text, 4, '0'),
  '水质样品-' || generate_series,
  '水质样品',
  '华泰检测有限公司',
  CURRENT_DATE - (random() * 30)::int,
  CURRENT_DATE - (random() * 5)::int,
  (random() * 1000)::numeric(10,2),
  'ml',
  'registered',
  '仓库-A-' || (random() * 20)::int,
  'normal',
  '样品外观正常',
  (SELECT id FROM users WHERE username = 'admin'),
  CURRENT_TIMESTAMP
FROM generate_series(1, 100);
```

### 3. 批量插入仪器

```sql
-- 生成50台仪器
INSERT INTO instruments (
  id, code, name, model, manufacturer, serial_number,
  purchase_date, status, location, responsible_person,
  next_calibration_date, specifications, notes, created_by, created_at
)
SELECT
  gen_random_uuid(),
  'INS' || to_char(CURRENT_DATE, 'YYYY') || lpad(generate_series::text, 4, '0'),
  '气相色谱仪-' || generate_series,
  'GC-' || (1000 + generate_series),
  '安捷伦',
  'SN' || (100000 + generate_series),
  CURRENT_DATE - (random() * 1825)::int,
  'IN_USE',
  '实验室-A-' || (generate_series % 10 + 1) || '号位',
  'admin',
  CURRENT_DATE + (random() * 365)::int,
  '精度: ±0.001, 量程: 1000',
  '状态良好',
  (SELECT id FROM users WHERE username = 'admin'),
  CURRENT_TIMESTAMP
FROM generate_series(1, 50);
```

## 🎨 前端分页配置

### Element Plus Table 分页示例

```vue
<template>
  <div>
    <!-- 表格 -->
    <el-table :data="tableData" style="width: 100%">
      <el-table-column prop="barcode" label="样品编号" />
      <el-table-column prop="sample_name" label="样品名称" />
      <el-table-column prop="sample_type" label="样品类型" />
      <el-table-column prop="status" label="状态" />
    </el-table>
    
    <!-- 分页 -->
    <el-pagination
      v-model:current-page="currentPage"
      v-model:page-size="pageSize"
      :page-sizes="[10, 15, 20, 50, 100]"
      :total="total"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleSizeChange"
      @current-change="handleCurrentChange"
    />
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const currentPage = ref(1)
const pageSize = ref(15)
const total = ref(0)
const tableData = ref([])

const fetchData = async () => {
  const response = await fetch(
    `http://localhost:8000/api/v1/samples?page=${currentPage.value}&page_size=${pageSize.value}`
  )
  const data = await response.json()
  tableData.value = data.items
  total.value = data.total
}

const handleSizeChange = (val) => {
  pageSize.value = val
  fetchData()
}

const handleCurrentChange = (val) => {
  currentPage.value = val
  fetchData()
}

onMounted(() => {
  fetchData()
})
</script>
```

## 📈 测试分页功能

### 1. 测试不同页码

- 第1页：显示前15条数据
- 第2页：显示第16-30条数据
- 最后一页：显示剩余数据

### 2. 测试不同每页数量

- 每页10条：总共15页（150条数据）
- 每页15条：总共10页
- 每页20条：总共8页
- 每页50条：总共3页

### 3. 测试排序功能

- 按创建时间排序（升序/降序）
- 按样品编号排序
- 按样品类型排序

### 4. 测试搜索功能

- 搜索样品名称
- 搜索样品编号
- 搜索客户名称
- 多条件组合搜索

## ⚠️ 注意事项

1. **数据清理**: 测试完成后，可以使用以下命令清理测试数据：
   ```sql
   DELETE FROM samples WHERE description = '样品外观正常';
   DELETE FROM instruments WHERE notes = '状态良好';
   ```

2. **性能测试**: 生成大量数据后，注意观察：
   - 页面加载速度
   - API响应时间
   - 数据库查询性能

3. **备份数据**: 在生成大量测试数据前，建议备份数据库：
   ```bash
   pg_dump -U postgres laboratory_db > backup.sql
   ```

## 🔍 故障排查

### 问题1：Python脚本无法运行

**解决方案**：
1. 检查Python环境是否激活
2. 检查依赖包是否安装：`pip install -r requirements.txt`
3. 检查数据库连接配置：`.env`文件

### 问题2：数据库连接失败

**解决方案**：
1. 检查数据库服务是否运行
2. 检查数据库连接字符串
3. 检查数据库用户权限

### 问题3：前端分页不显示

**解决方案**：
1. 检查API返回的数据格式
2. 检查分页组件的绑定
3. 检查total值是否正确

## 📞 获取帮助

如果遇到问题，请检查：
1. 后端日志：`backend-api/logs/`
2. 前端控制台：浏览器开发者工具
3. 数据库日志

---

**文档版本**: v1.0.0
**更新时间**: 2026-05-12
