# 报告生成功能完善说明

## ✅ 已完成的工作

### 1. 专业报告模板

已创建3个专业的中文报告模板，包含完整的HTML样式和结构：

#### 分析报告模板 (ANALYSIS_REPORT)
- **用途**: 分析级别的检测报告
- **包含内容**:
  - 基本信息表格（报告编号、样品条码、样品名称、样品类型、委托单位、接收日期、检测日期）
  - 检测结果表格（序号、检测项目、检测方法、检测结果、单位、标准限值、判定）
  - 分析说明段落
  - 签名区域（分析人员、审核人员及日期）
- **变量数量**: 13个

#### 样品报告模板 (SAMPLE_REPORT)
- **用途**: 样品级别的检测报告
- **包含内容**:
  - 详细样品信息表格（报告编号、样品编号、样品条码、样品名称、样品类型、样品数量、委托单位、联系方式、接收日期、采样日期、采样地点）
  - 检测数据表格（序号、检测项目、检测结果、单位、检测方法）
  - 检测人员信息和备注
- **变量数量**: 16个

#### 技术报告模板 (TECHNICAL_REPORT)
- **用途**: 技术级别的检测报告
- **包含内容**:
  - 报告信息表格
  - 检测方法与标准表格（检测项目、检测方法、检测标准、方法依据）
  - 检测结果与评估表格（检测项目、检测结果、单位、标准要求、符合性评价）
  - 技术分析段落
  - 结论与建议段落
  - 技术负责人签名
- **变量数量**: 11个

### 2. 模板特点

✅ **专业的HTML结构**
- 使用标准的HTML5文档结构
- 包含完整的CSS样式
- 适合打印和导出

✅ **中文界面**
- 所有标题和标签使用中文
- 符合国内实验室报告规范

✅ **完整的样式**
- 表格边框和间距
- 标题样式和层级
- 打印优化样式

✅ **变量占位符**
- 使用 `{{variableName}}` 格式
- 支持数组循环 `{{#each results}}`
- 支持对象属性访问 `{{sample.barcode}}`

### 3. 后端实现

✅ **报告生成服务** (`app/services/report_service.py`)
- `generate_report()`: 生成报告的主方法
- `_fetch_report_data()`: 获取报告所需的所有数据
- `_fill_report_template()`: 填充模板变量
- `_generate_report_number()`: 生成报告编号

✅ **报告路由** (`app/routers/reports.py`)
- `POST /api/v1/reports/generate`: 生成报告
- `GET /api/v1/reports`: 查询报告列表
- `GET /api/v1/reports/{id}`: 获取报告详情

### 4. 前端实现

✅ **报告生成页面** (`vue-project/src/views/report/ReportGenerator.vue`)
- 样品选择区域（支持搜索）
- 模板选择区域（支持筛选）
- 报告预览区域
- 报告编辑功能（富文本编辑器）
- 打印功能
- 导出功能（PDF/Word）
- 电子签名功能

## 🔧 需要完善的功能

### 1. 模板变量填充逻辑

当前后端的 `_fill_report_template()` 方法需要实现以下功能：

```python
def _fill_report_template(
    self,
    template_content: str,
    template_variables: List[Dict],
    report_data: ReportData,
    report_number: str = None
) -> str:
    """
    填充报告模板
    
    需要实现：
    1. 替换简单变量 {{variableName}}
    2. 替换对象属性 {{sample.barcode}}
    3. 处理数组循环 {{#each results}}...{{/each}}
    4. 处理条件判断 {{#if condition}}...{{/if}}
    """
    # 实现模板引擎逻辑
    pass
```

**建议使用模板引擎**:
- **Jinja2**: Python标准模板引擎
- **Handlebars.py**: 与前端Handlebars兼容
- **Mako**: 高性能模板引擎

### 2. 报告数据结构

确保 `ReportData` 包含所有必要的字段：

```python
class ReportData(BaseModel):
    """报告数据"""
    # 报告基本信息
    reportNumber: Optional[str] = None
    
    # 样品信息
    sample: Dict[str, Any]  # 包含所有样品字段
    
    # 检测结果
    results: List[Dict[str, Any]]  # 检测结果列表
    
    # 检测方法
    methods: Optional[List[Dict[str, Any]]] = None
    
    # 人员信息
    analyst: Optional[str] = None
    reviewer: Optional[str] = None
    tester: Optional[str] = None
    technicalLead: Optional[str] = None
    
    # 日期信息
    testDate: Optional[date] = None
    analysisDate: Optional[date] = None
    reviewDate: Optional[date] = None
    
    # 分析和结论
    analysisDescription: Optional[str] = None
    technicalAnalysis: Optional[str] = None
    conclusion: Optional[str] = None
    recommendation: Optional[str] = None
    remarks: Optional[str] = None
```

### 3. 前端服务对接

确保前端的 `reportService.generateReport()` 正确调用后端API：

```typescript
// vue-project/src/services/reportService.ts
export async function generateReport(data: {
  sampleId: string
  templateId: string
  preview: boolean
}): Promise<{
  reportId?: string
  reportNumber?: string
  content: string
}> {
  const response = await http.post('/reports/generate', data)
  return response.data
}
```

### 4. 报告编号生成规则

实现 `_generate_report_number()` 方法：

```python
async def _generate_report_number(
    self,
    db: AsyncSession,
    sample_id: str
) -> str:
    """
    生成报告编号
    
    格式建议: RPT-YYYYMMDD-XXXX
    例如: RPT-20260505-0001
    """
    from datetime import datetime
    
    # 获取今天的日期
    today = datetime.now().strftime("%Y%m%d")
    
    # 查询今天已生成的报告数量
    result = await db.execute(
        select(func.count(Report.id))
        .where(Report.report_number.like(f"RPT-{today}-%"))
    )
    count = result.scalar() or 0
    
    # 生成新的报告编号
    sequence = str(count + 1).zfill(4)
    return f"RPT-{today}-{sequence}"
```

## 📋 使用流程

### 1. 添加专业模板

```bash
cd fastapi-backend
.\venv\Scripts\python.exe add-professional-templates.py
```

**输出**:
```
已添加: 分析报告模板
已添加: 样品报告模板
已添加: 技术报告模板

成功添加 3 个专业模板！
```

### 2. 验证模板

```bash
.\venv\Scripts\python.exe check-report-templates.py
```

### 3. 在前端生成报告

1. 访问: `http://localhost:5173/report/generator`
2. 在左侧选择一个样品
3. 选择一个报告模板
4. 点击"生成报告"按钮
5. 在右侧查看生成的报告
6. 可以编辑、打印或导出报告

## 🎯 下一步工作

### 优先级1: 实现模板引擎

**选择方案**: 使用 Jinja2

**安装依赖**:
```bash
pip install jinja2
```

**实现代码**:
```python
from jinja2 import Template

def _fill_report_template(
    self,
    template_content: str,
    template_variables: List[Dict],
    report_data: ReportData,
    report_number: str = None
) -> str:
    """使用Jinja2填充模板"""
    
    # 准备模板数据
    template_data = {
        "reportNumber": report_number,
        "sampleBarcode": report_data.sample.get("barcode"),
        "sampleName": report_data.sample.get("sampleName"),
        "sampleType": report_data.sample.get("sampleType"),
        "clientName": report_data.sample.get("clientName"),
        "receivedDate": report_data.sample.get("receivedDate"),
        "testDate": report_data.testDate,
        "results": report_data.results,
        "analyst": report_data.analyst,
        "analysisDate": report_data.analysisDate,
        "analysisDescription": report_data.analysisDescription,
        "reviewer": report_data.reviewer,
        "reviewDate": report_data.reviewDate,
        # ... 其他变量
    }
    
    # 渲染模板
    template = Template(template_content)
    return template.render(**template_data)
```

### 优先级2: 完善报告数据获取

确保 `_fetch_report_data()` 返回所有必要的数据：

```python
async def _fetch_report_data(
    self,
    db: AsyncSession,
    sample_id: str,
    user_id: str
) -> ReportData:
    """获取报告数据"""
    
    # 1. 获取样品信息
    sample = await self._get_sample_with_relations(db, sample_id)
    
    # 2. 获取检测结果
    results = await self._get_test_results(db, sample_id)
    
    # 3. 获取检测方法
    methods = await self._get_test_methods(db, sample_id)
    
    # 4. 获取人员信息
    personnel = await self._get_personnel_info(db, sample_id, user_id)
    
    # 5. 构建报告数据
    return ReportData(
        sample=sample,
        results=results,
        methods=methods,
        **personnel
    )
```

### 优先级3: 添加更多模板

创建质量报告和综合报告模板：

```python
# 在 add-professional-templates.py 中添加
QUALITY_TEMPLATE = """..."""
COMPREHENSIVE_TEMPLATE = """..."""
```

### 优先级4: 实现PDF导出

使用 `weasyprint` 或 `pdfkit` 将HTML转换为PDF：

```python
from weasyprint import HTML

async def export_report_to_pdf(
    self,
    report_id: str,
    db: AsyncSession
) -> bytes:
    """导出报告为PDF"""
    
    # 获取报告
    report = await self.get_report(db, report_id)
    
    # 转换为PDF
    pdf_bytes = HTML(string=report.content).write_pdf()
    
    return pdf_bytes
```

## 📝 测试清单

- [ ] 模板已成功添加到数据库
- [ ] 前端可以获取模板列表
- [ ] 前端可以选择样品和模板
- [ ] 点击"生成报告"按钮可以调用后端API
- [ ] 后端可以正确填充模板变量
- [ ] 生成的报告内容正确显示
- [ ] 报告可以编辑
- [ ] 报告可以打印
- [ ] 报告可以导出为PDF
- [ ] 报告可以添加电子签名

## 🎨 模板样式优化建议

### 1. 添加页眉页脚

```html
<style>
@media print {
  @page {
    margin: 2cm;
    @top-center {
      content: "检测报告";
    }
    @bottom-right {
      content: "第 " counter(page) " 页";
    }
  }
}
</style>
```

### 2. 添加水印

```html
<style>
.watermark {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-45deg);
  font-size: 100px;
  color: rgba(0, 0, 0, 0.05);
  z-index: -1;
}
</style>
<div class="watermark">草稿</div>
```

### 3. 添加二维码

可以在报告中添加二维码，用于验证报告真伪：

```html
<div class="qr-code">
  <img src="data:image/png;base64,{{qrCodeData}}" alt="验证二维码" />
  <p>扫描验证报告真伪</p>
</div>
```

## 📚 相关文档

- [Jinja2文档](https://jinja.palletsprojects.com/)
- [WeasyPrint文档](https://weasyprint.org/)
- [Element Plus表格组件](https://element-plus.org/zh-CN/component/table.html)
- [Quill富文本编辑器](https://quilljs.com/)

---

**创建时间**: 2026-05-05  
**状态**: 🚧 进行中  
**下一步**: 实现模板引擎和变量填充逻辑
