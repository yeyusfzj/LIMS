# 前端调试指南 - 详细版

## 问题描述
样品编辑后保存，前端显示成功，但数据库中的数据没有更新。

## 已添加的调试日志

### 1. sample.ts 中的日志
- `💾 update API 请求 - 原始数据`: 显示从组件传入的原始数据（驼峰命名）
- `💾 update API 请求 - updateData详情`: JSON格式显示原始数据
- `💾 转换后的数据（蛇形命名）`: 显示转换后的数据
- `💾 转换后的数据（JSON）`: JSON格式显示转换后的数据
- `💾 update API 响应`: 显示后端返回的响应
- `💾 update 提取的 item`: 显示从响应中提取的样品数据

### 2. http.ts 中的日志
- `🌐 HTTP请求拦截器 - 发送请求`: 显示实际发送到后端的请求详情
  - url: 请求路径
  - method: 请求方法
  - data: 请求体数据
  - dataJSON: JSON格式的请求体数据

## 测试步骤

### 步骤 1: 硬刷新浏览器
**非常重要！** 必须硬刷新浏览器以加载最新的前端代码：
- Windows/Linux: `Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac: `Cmd + Shift + R`

### 步骤 2: 打开浏览器开发者工具
1. 按 `F12` 打开开发者工具
2. 切换到 "Console" 标签页
3. 清空之前的日志（点击 🚫 图标）

### 步骤 3: 执行编辑操作
1. 登录系统（用户名: `admin`, 密码: `admin123`）
2. 进入样品管理页面
3. 点击某个样品的"查看"按钮
4. 点击"编辑"按钮
5. 修改样品名称（例如改为 "测试111"）
6. 点击"保存"按钮

### 步骤 4: 查看控制台日志
按照以下顺序查看日志输出：

#### 4.1 SampleRegistration.vue 的日志
```
💾 开始更新样品，ID: xxx
📝 更新数据: {id: 'xxx', clientName: 'xxx', sampleName: '测试111', ...}
```
**检查点**: `sampleName` 字段的值是否是你输入的新名称？

#### 4.2 sample.ts 的日志
```
💾 update API 请求 - 原始数据: {id: 'xxx', updateData: {...}}
💾 update API 请求 - updateData详情: {
  "clientName": "xxx",
  "sampleName": "测试111",
  ...
}
💾 转换后的数据（蛇形命名）: {client_name: 'xxx', sample_name: '测试111', ...}
💾 转换后的数据（JSON）: {
  "client_name": "xxx",
  "sample_name": "测试111",
  ...
}
```
**检查点**: 
- `updateData详情` 中是否包含 `sampleName: "测试111"`？
- `转换后的数据` 中是否包含 `sample_name: "测试111"`？

#### 4.3 http.ts 的日志
```
🌐 HTTP请求拦截器 - 发送请求: {
  url: '/samples/xxx',
  method: 'patch',
  data: {client_name: 'xxx', sample_name: '测试111', ...},
  dataJSON: "{\"client_name\":\"xxx\",\"sample_name\":\"测试111\",...}"
}
```
**关键检查点**: 
- `data` 对象中是否包含 `sample_name: "测试111"`？
- `dataJSON` 字符串中是否包含 `"sample_name":"测试111"`？

#### 4.4 后端响应日志
```
收到响应: {status: 200, statusText: 'OK', data: {...}}
💾 update API 响应: {success: true, data: {...}, message: '样品更新成功'}
💾 update 提取的 item: {sample_name: '测试111', ...}
```
**检查点**: 
- 响应中的 `sample_name` 是否是新的值 "测试111"？

### 步骤 5: 检查后端日志
在后端终端中查看日志：
```
INFO:     开始更新样品: sample_id=xxx
INFO:     接收到的更新数据: {'client_name': 'xxx', 'sample_name': '测试111', ...}
INFO:     过滤后的更新数据: {'client_name': 'xxx', 'sample_name': '测试111', ...}
INFO:     样品更新成功: ID=xxx, 条码=xxx, 版本=xx
```
**关键检查点**: 
- `接收到的更新数据` 中是否包含 `'sample_name': '测试111'`？
- 如果没有，说明前端没有正确发送数据

### 步骤 6: 验证数据库
运行数据库检查脚本：
```bash
cd fastapi-backend
.\.venv\Scripts\python.exe check_sample_data.py
```

查看输出中的 `sample_name` 字段是否已更新。

## 可能的问题和解决方案

### 问题 1: 浏览器缓存
**症状**: 日志格式没有变化，还是旧的日志
**解决**: 硬刷新浏览器（Ctrl + Shift + R）

### 问题 2: 字段转换失败
**症状**: `转换后的数据` 中没有 `sample_name` 字段
**原因**: `updateData` 中没有 `sampleName` 字段
**解决**: 检查 `SampleRegistration.vue` 中传递给 `updateSample` 的数据

### 问题 3: HTTP 请求数据丢失
**症状**: `http.ts` 日志中的 `data` 对象为空或缺少字段
**原因**: axios 请求拦截器可能修改了数据
**解决**: 检查 `http.ts` 中的请求拦截器

### 问题 4: 后端没有收到数据
**症状**: 后端日志显示 `接收到的更新数据: {}`
**原因**: 前端发送的数据格式不正确
**解决**: 检查前端日志中的 `dataJSON` 字段

## 下一步行动

请按照上述步骤执行测试，并将以下信息反馈给我：

1. **步骤 4.2 的日志**: `转换后的数据（JSON）` 的完整内容
2. **步骤 4.3 的日志**: `dataJSON` 的完整内容
3. **步骤 5 的日志**: 后端 `接收到的更新数据` 的完整内容

这样我就能准确定位问题所在。
