# 审核任务问题诊断和修复总结

## 问题报告

**时间**: 2026-05-08  
**报告人**: 用户  
**问题**: 审核任务查询失败，返回 500 错误

**错误信息**:
```
column audit_tasks.sample_id does not exist
```

## 诊断过程

### 第一步：检查错误日志

从后端日志中发现：
```
[ERROR] column audit_tasks.sample_id does not exist
[SQL: SELECT audit_tasks.id, audit_tasks.sample_id, audit_tasks.level, ...
```

**初步判断**: 列名映射问题

### 第二步：检查数据库实际结构

创建检查脚本 `check_db_columns.py` 和 `check_all_tables.py`，发现：

**audit_tasks 表的实际列**:
- ✅ `id`, `level`, `auditorId`, `status`, `decision`, `comments`
- ✅ `submittedAt`, `completedAt`
- ✅ `taskId` ← **关键发现**
- ❌ **没有** `sampleId` 列

**命名约定**:
- 所有列都使用**驼峰命名**（camelCase）
- 不是蛇形命名（snake_case）

### 第三步：检查 Prisma Schema

查看 `backend-api-deprecated/prisma/schema.prisma`，发现正确的模型定义：

```prisma
model AuditTask {
  id        String         @id @default(uuid())
  taskId    String         ← 关联到 Task 表，不是 Sample 表
  task      Task           @relation(fields: [taskId], references: [id])
  level     Int
  auditorId String
  status    AuditStatus
  ...
}
```

**关键发现**:
- `AuditTask` 通过 `taskId` 关联到 `Task` 表
- `Task` 通过 `instanceId` 关联到 `WorkflowInstance` 表
- `WorkflowInstance` 通过 `sampleId` 关联到 `Sample` 表
- **关联路径**: `AuditTask → Task → WorkflowInstance → Sample`

### 第四步：识别根本原因

**根本原因**:
1. SQLAlchemy 模型 (`audit.py`) 错误地定义了 `sampleId` 字段
2. 实际数据库中应该是 `taskId` 字段
3. 审核任务不直接关联到样品，而是通过工作流任务关联

## 修复过程

### 修复 1: 更正 AuditTask 模型

**文件**: `backend-api/app/models/audit.py`

**修改内容**:
```python
# ❌ 修复前
class AuditTask(Base):
    sampleId = Column('sample_id', String, ForeignKey('samples.id'), ...)
    sample = relationship('Sample', back_populates='audit_tasks')

# ✅ 修复后
class AuditTask(Base):
    taskId = Column(String, ForeignKey('tasks.id', ondelete='CASCADE'), ...)
    task = relationship('Task', back_populates='audit_tasks')
```

**关键变更**:
- 将 `sampleId` 改为 `taskId`
- 外键指向 `tasks.id` 而不是 `samples.id`
- 关系指向 `Task` 而不是 `Sample`
- 移除了蛇形命名的列名映射

### 修复 2: 移除其他模型的列名映射

由于数据库使用驼峰命名，移除了以下模型的列名映射：

**AuditCommentTemplate**:
- `usageCount`, `isDefault`, `createdBy`, `createdAt`, `updatedAt`

**AuditWorkflowConfig**:
- `sampleTypes`, `parallelAudit`, `createdBy`, `createdAt`, `updatedAt`

**AuditHistory**:
- `taskId`, `performedBy`, `performedAt`

### 修复 3: 更新 Sample 模型

**文件**: `backend-api/app/models/sample.py`

**修改内容**:
```python
# ❌ 移除错误的关系
# audit_tasks = relationship('AuditTask', back_populates='sample')

# ✅ 添加注释说明
# 注意：audit_tasks 关联到 Task 表，而不是直接关联到 Sample
# 可以通过 workflow_instance.tasks 访问审核任务
```

### 修复 4: 更新 Task 模型

**文件**: `backend-api/app/models/task.py`

**修改内容**:
```python
# ✅ 添加关系
audit_tasks = relationship('AuditTask', back_populates='task', cascade='all, delete-orphan')
```

## 验证结果

### ✅ 后端启动成功

```
[INFO] 2026-05-08 14:13:40 - app.main - Application startup complete
INFO:     Application startup complete.
```

- ✅ 没有关系错误
- ✅ 没有列名映射错误
- ✅ 派工引擎初始化成功
- ✅ 数据库连接正常

### ⚠️ 审核服务需要更新

虽然模型定义已修复，但审核服务 (`audit_service.py`) 中的代码仍然假设 `AuditTask` 直接关联到 `Sample`，需要进一步修复。

## 当前状态

### ✅ 已完成

1. **模型定义修复**
   - `AuditTask` 模型正确关联到 `Task` 表
   - `Task` 模型添加了 `audit_tasks` 关系
   - `Sample` 模型移除了错误的 `audit_tasks` 关系
   - 移除了不必要的列名映射

2. **后端启动**
   - 后端成功启动，没有模型错误
   - 数据库连接正常

3. **文档创建**
   - ✅ `审核任务模型修复报告.md` - 详细的修复过程
   - ✅ `审核任务API修复建议.md` - 下一步修复方案
   - ✅ `审核任务问题诊断和修复总结.md` - 本文档

### ⚠️ 待完成

1. **审核服务更新**
   - 需要更新 `audit_service.py` 中的所有方法
   - 需要创建 `WorkflowInstance` 模型
   - 需要更新查询逻辑以适应正确的关联路径

2. **API 测试**
   - 测试审核任务列表查询
   - 测试审核任务创建
   - 测试审核任务详情
   - 测试审核任务执行

## 数据库结构总结

### 正确的关联路径

```
AuditTask → Task → WorkflowInstance → Sample
```

### 表关系

| 表 | 外键 | 关联到 |
|----|------|--------|
| `audit_tasks` | `taskId` | `tasks.id` |
| `tasks` | `instanceId` | `workflow_instances.id` |
| `workflow_instances` | `sampleId` | `samples.id` |

### 命名约定

| 项目 | 约定 | 示例 |
|------|------|------|
| 数据库列名 | 驼峰命名 | `auditorId`, `submittedAt` |
| Python 属性名 | 驼峰命名 | `auditorId`, `submittedAt` |
| 列名映射 | 不需要 | - |

## 如何访问审核任务

### 方法 1: 通过 Sample 访问

```python
sample = await db.get(Sample, sample_id)
if sample.workflow_instance:
    for task in sample.workflow_instance.tasks:
        for audit_task in task.audit_tasks:
            print(f"审核任务: {audit_task.id}")
```

### 方法 2: 直接查询

```python
from sqlalchemy import select
from sqlalchemy.orm import joinedload

stmt = (
    select(AuditTask)
    .join(Task)
    .join(WorkflowInstance)
    .where(WorkflowInstance.sampleId == sample_id)
    .options(joinedload(AuditTask.task))
)
audit_tasks = await db.execute(stmt)
```

### 方法 3: 通过 Task 访问

```python
task = await db.get(Task, task_id)
for audit_task in task.audit_tasks:
    print(f"审核任务: {audit_task.id}")
```

## 经验教训

### ❌ 错误做法

1. **假设数据库命名约定**
   - 没有先检查数据库实际结构
   - 基于错误的假设添加列名映射

2. **忽略 Prisma Schema**
   - 项目使用 Prisma 作为主要 ORM
   - 应该参考 Prisma schema 定义模型

3. **不完整的模型定义**
   - 只定义了部分模型
   - 没有定义完整的关系链

### ✅ 正确做法

1. **先检查数据库实际结构**
   - 使用 `information_schema.columns` 查询
   - 使用 SQLAlchemy `inspect` 检查

2. **参考 Prisma Schema**
   - Prisma schema 是数据库结构的权威定义
   - SQLAlchemy 模型应该与 Prisma schema 一致

3. **理解数据关系**
   - 理解表之间的关联路径
   - 正确定义外键和关系

4. **验证修复**
   - 检查后端启动日志
   - 验证没有关系错误
   - 测试 API 端点

## 下一步行动

### 选项 1: 完整修复（推荐）

**工作内容**:
1. 创建 `WorkflowInstance` 模型
2. 更新 `audit_service.py` 中的所有方法
3. 更新查询逻辑以适应正确的关联路径
4. 全面测试审核功能

**时间估计**: 2-4 小时  
**风险**: 中等  
**收益**: 高（符合正确的数据库结构）

### 选项 2: 临时修复

**工作内容**:
1. 只修复 `list_audit_tasks` 方法
2. 添加 JOIN 查询以获取样品信息
3. 让前端能够查询审核任务

**时间估计**: 30 分钟  
**风险**: 低  
**收益**: 低（只能查询，不能创建）

## 相关文件

### 已修复的文件
- ✅ `backend-api/app/models/audit.py`
- ✅ `backend-api/app/models/sample.py`
- ✅ `backend-api/app/models/task.py`

### 待修复的文件
- ⚠️ `backend-api/app/services/audit_service.py`
- ⚠️ `backend-api/app/models/workflow.py` (需要创建)

### 工具脚本
- ✅ `backend-api/check_db_columns.py`
- ✅ `backend-api/check_all_tables.py`

### 文档
- ✅ `审核任务模型修复报告.md`
- ✅ `审核任务API修复建议.md`
- ✅ `审核任务问题诊断和修复总结.md`
- ✅ `列名映射问题最终修复报告.md`

---

**诊断完成时间**: 2026-05-08 14:15  
**诊断者**: AI 助手  
**状态**: 模型修复完成，等待用户确认下一步修复方案
