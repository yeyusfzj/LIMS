# 实验室管理系统 - 启动指南

## 🚀 快速启动

### 当前运行状态

✅ **后端服务**: 运行中 (http://localhost:3000)  
✅ **前端服务**: 运行中 (http://localhost:5173)

### 访问地址

- **前端应用**: http://localhost:5173
- **后端 API**: http://localhost:3000
- **API 文档**: http://localhost:3000/api-docs
- **健康检查**: http://localhost:3000/health

## 👤 登录账号

### 管理员账号
```
用户名: admin
密码: Admin@123456
```

### 测试账号
```
用户名: testuser
密码: User@123456
```

## 📝 手动启动服务

如果服务未运行,可以使用以下命令启动:

### 启动后端
```bash
cd backend-api
npm run dev
```

### 启动前端
```bash
cd vue-project
npm run dev
```

## 🛠️ 开发命令

### 后端命令
```bash
# 开发模式
npm run dev

# 构建
npm run build

# 运行测试
npm run test

# 数据库迁移
npm run prisma:migrate

# 数据库种子
npm run prisma:seed

# 打开 Prisma Studio
npm run prisma:studio
```

### 前端命令
```bash
# 开发模式
npm run dev

# 构建
npm run build

# 预览构建
npm run preview

# 运行测试
npm run test
```

## 🔧 环境配置

### 后端环境变量 (.env)
```env
NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://postgres:password@localhost:5432/lims_dev
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET=dev-secret-key-12345
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
```

### 前端配置
前端会自动连接到 `http://localhost:3000` 的后端服务。

## 📊 系统架构

```
┌─────────────────┐         ┌─────────────────┐
│   Vue 前端      │ ◄─────► │  Express 后端   │
│  (Port 5173)    │  HTTP   │  (Port 3000)    │
└─────────────────┘         └─────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
              ┌─────▼─────┐   ┌─────▼─────┐   ┌─────▼─────┐
              │ PostgreSQL │   │   Redis   │   │   Bull    │
              │  数据库    │   │   缓存    │   │  队列     │
              └───────────┘   └───────────┘   └───────────┘
```

## 🧪 测试连接

运行连接测试脚本:
```bash
node test-backend-connection.js
```

测试结果应显示所有测试通过 (6/6)。

## 📚 主要功能模块

1. **样品管理** - 样品登记、追踪、转移
2. **工作流管理** - 工作流设计、任务分配
3. **检测结果** - 结果录入、公式计算
4. **审核管理** - 审核任务、审批流程
5. **报告管理** - 报告生成、电子签名
6. **统计分析** - 数据统计、报表导出
7. **系统管理** - 用户、角色、权限管理

## 🔍 故障排查

### 后端无法启动
1. 检查 PostgreSQL 是否运行
2. 检查 Redis 是否运行
3. 检查端口 3000 是否被占用
4. 查看 `backend-api/logs/error.log`

### 前端无法访问后端
1. 检查后端是否正常运行
2. 检查 CORS 配置
3. 检查网络请求是否被拦截
4. 查看浏览器控制台错误

### 数据库连接失败
1. 检查 DATABASE_URL 配置
2. 确认数据库服务运行中
3. 运行数据库迁移: `npm run prisma:migrate`
4. 运行种子数据: `npm run prisma:seed`

## 📖 相关文档

- [后端 API 文档](backend-api/README.md)
- [前端开发指南](vue-project/README.md)
- [数据库设计](backend-api/docs/DATABASE_SETUP.md)
- [架构说明](backend-api/docs/ARCHITECTURE.md)
- [测试报告](后端连接测试报告.md)

## 💡 提示

- 首次启动需要运行数据库迁移和种子数据
- 开发时建议同时打开前后端的终端窗口
- 可以使用 Prisma Studio 查看和管理数据库数据
- API 文档提供了所有接口的详细说明和测试功能

---

**最后更新**: 2026-04-11  
**系统版本**: v1.0.0
