# 登录 401 错误解决方案

## 问题总结

用户使用前端登录页面尝试登录时收到 401 Unauthorized 错误。

## 根本原因

**FastAPI 后端与 Node.js 后端共享同一个 PostgreSQL 数据库** (`lims_dev`)。

- FastAPI 后端配置：`DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/lims_dev`
- Node.js 后端配置：使用相同的数据库 `lims_dev`
- 数据库中的用户数据来自 Node.js 后端的种子数据
- 实际密码：`Admin@123456`（不是 FastAPI 初始化脚本中的 `admin123`）

## 解决方案

已更新前端登录页面的默认密码提示：

**文件**：`vue-project/src/views/Login.vue`

```html
<!-- 修改后 -->
<p>默认账号：admin / Admin@123456</p>
```

## 正确的登录凭据

### 管理员账号
- **用户名**：`admin`
- **密码**：`Admin@123456` ✅
- **角色**：系统管理员

### 测试账号
- **用户名**：`testuser`
- **密码**：`User@123456`
- **角色**：普通用户 + 实验室技术员

## 测试验证

已通过测试脚本验证登录成功：

```bash
node test-fastapi-login.js
```

测试结果：
```
测试登录: admin / Admin@123456
✓ 登录成功!
状态码: 200
用户信息: {
  id: '73aaaa86-961e-4333-8edf-3dc48dfec25c',
  username: 'admin',
  email: 'admin@example.com',
  fullName: '系统管理员',
  roles: [ 'admin' ]
}
Token: 已获取
```

## 更新的文件

1. **前端登录页面**：`vue-project/src/views/Login.vue`
   - 更新默认密码提示为 `Admin@123456`

2. **Bugfix 文档**：`.kiro/specs/login-401-authentication-fix/bugfix.md`
   - 记录问题根本原因
   - 记录数据库共享情况
   - 更新测试验证结果

3. **Steering 文件**：`.kiro/steering/backend-selection.md`
   - 更新默认账号信息
   - 说明数据库共享情况
   - 更新常见问题解答

## 使用说明

现在可以使用以下凭据登录系统：

1. 打开浏览器访问：`http://localhost:5173/login`
2. 输入用户名：`admin`
3. 输入密码：`Admin@123456`
4. 点击登录按钮

登录成功后会自动跳转到系统首页。

## 重要提示

- FastAPI 后端（端口 8000）与 Node.js 后端（端口 3000）共享同一个数据库
- 两个后端使用相同的用户数据和密码
- 前端当前配置连接到 FastAPI 后端（端口 8000）
- 如果需要修改密码，需要在数据库中直接修改，或使用 Node.js 后端的种子脚本重新初始化
