# 样品流转400错误修复 - 实现任务列表

## 任务概述

本任务列表遵循Bug条件探索性修复工作流程，确保系统性验证：
- **修复检查**: 验证Bug对所有有问题的输入都已修复
- **保持不变检查**: 验证现有行为保持不变

## 实现计划

- [ ] 1. 编写Bug条件探索测试
  - **Property 1: Bug Condition** - 流转查询参数验证失败
  - **重要**: 此测试必须在未修复代码上失败 - 失败确认Bug存在
  - **不要尝试修复测试或代码当它失败时**
  - **注意**: 此测试编码了预期行为 - 在实现后通过时将验证修复
  - **目标**: 展示证明Bug存在的反例
  - **作用域PBT方法**: 对于确定性Bug，将属性作用域限定为具体失败案例以确保可重现性
  - 测试包含查询参数的流转列表请求返回400错误（来自设计中的Bug条件）
  - 测试断言应匹配设计中的预期行为属性
  - 在未修复代码上运行测试
  - **预期结果**: 测试失败（这是正确的 - 证明Bug存在）
  - 记录发现的反例以理解根本原因
  - 当测试编写、运行并记录失败时标记任务完成
  - _Requirements: 2.1, 2.2_

- [ ] 2. 编写保持不变属性测试（在实现修复之前）
  - **Property 2: Preservation** - 非流转查询功能保持不变
  - **重要**: 遵循观察优先方法论
  - 在未修复代码上观察非Bug输入的行为
  - 编写基于属性的测试捕获来自保持不变要求的观察行为模式
  - 基于属性的测试生成许多测试用例以提供更强保证
  - 在未修复代码上运行测试
  - **预期结果**: 测试通过（这确认了要保持的基线行为）
  - 当测试编写、运行并在未修复代码上通过时标记任务完成
  - _Requirements: 3.1, 3.2, 3.3_
- [ ] 3. 样品流转400错误修复

  - [ ] 3.1 后端API修复 - 添加查询参数验证中间件
    - 在`backend-api/src/validators/sampleValidator.ts`中创建`queryTransferSchema`验证规则
    - 验证分页参数(`page`, `pageSize`)的类型转换和范围
    - 验证搜索参数(`sampleNumber`, `status`)的格式和长度
    - 验证日期参数(`startDate`, `endDate`)的ISO格式和有效性
    - 在`backend-api/src/routes/sampleRoutes.ts`中为流转列表路由添加验证中间件
    - 使用`validateRequest(queryTransferSchema, 'query')`确保参数正确验证
    - _Bug_Condition: isBugCondition(input) 其中 input.query 包含无效格式的查询参数_
    - _Expected_Behavior: expectedBehavior(result) 来自设计 - API返回200状态码和数据_
    - _Preservation: 保持不变要求来自设计 - 其他样品管理功能继续正常工作_
    - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3_

  - [ ] 3.2 前端修复 - 参数格式和错误处理
    - 修复`vue-project/src/views/sample/SampleTransferManagement.vue`中的日期参数格式
    - 将`dateRange`数组转换为`startDate`和`endDate`分别传递
    - 确保使用ISO日期格式发送日期参数
    - 移除空值参数，避免发送undefined或null值
    - 添加更详细的400错误处理和用户友好提示
    - 确保分页参数作为数字类型发送
    - _Bug_Condition: 前端发送格式不正确的查询参数_
    - _Expected_Behavior: 前端发送正确格式的参数并处理错误响应_
    - _Preservation: 其他前端功能和页面导航保持不变_
    - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3_

  - [ ] 3.3 验证Bug条件探索测试现在通过
    - **Property 1: Expected Behavior** - 流转查询参数正确处理
    - **重要**: 重新运行步骤1中的相同测试 - 不要编写新测试
    - 步骤1中的测试编码了预期行为
    - 当此测试通过时，确认预期行为得到满足
    - 运行步骤1中的Bug条件探索测试
    - **预期结果**: 测试通过（确认Bug已修复）
    - _Requirements: 设计中的预期行为属性_

  - [ ] 3.4 验证保持不变测试仍然通过
    - **Property 2: Preservation** - 非流转查询功能保持不变
    - **重要**: 重新运行步骤2中的相同测试 - 不要编写新测试
    - 运行步骤2中的保持不变属性测试
    - **预期结果**: 测试通过（确认无回归）
    - 确认修复后所有测试仍然通过（无回归）

- [ ] 4. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有问题请询问用户。