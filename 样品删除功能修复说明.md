# 样品删除功能修复说明

## 问题描述
样品列表中无法删除样品,因为后端没有实现删除API。

## 根本原因
后端缺少删除功能的完整实现:
- ❌ 服务层没有删除方法
- ❌ 控制器没有删除处理器
- ❌ 路由没有配置DELETE端点

## 修复内容

### 1. 服务层 (`backend-api/src/services/sampleService.ts`)

添加了两个方法:

#### `deleteSample(id: string)`
- 软删除实现(更新状态为ARCHIVED,不物理删除)
- 检查样品是否存在
- 检查关联数据(检测结果、审核任务、报告)
- 如果有关联数据,拒绝删除并返回错误信息
- 记录删除日志

#### `batchDeleteSamples(ids: string[])`
- 批量删除多个样品
- 逐个处理,记录成功和失败数量
- 返回详细的操作结果

### 2. 控制器层 (`backend-api/src/controllers/sampleController.ts`)

添加了两个处理器:

#### `deleteSample(req, res)`
- 处理单个样品删除请求
- 错误分类处理:
  - 404: 样品不存在
  - 400: 有关联数据无法删除
  - 500: 服务器错误

#### `batchDeleteSamples(req, res)`
- 处理批量删除请求
- 验证请求参数
- 返回批量操作结果统计

### 3. 路由层 (`backend-api/src/routes/sampleRoutes.ts`)

添加了两个路由:

#### `DELETE /api/samples/:id`
- 删除单个样品
- 需要 `sample:delete` 权限
- 参数验证: UUID格式

#### `POST /api/samples/batch-delete`
- 批量删除样品
- 需要 `sample:delete` 权限
- 请求体: `{ ids: string[] }`

## 删除逻辑说明

### 软删除策略
采用软删除而非物理删除:
- ✅ 保留审计记录
- ✅ 可以恢复数据
- ✅ 保持数据完整性
- ✅ 符合实验室管理规范

### 删除前检查
删除前会检查以下关联数据:
1. **检测结果** (`testResults`)
   - 如果有结果,拒绝删除
   - 提示: "该样品已有检测结果,无法删除"

2. **审核任务** (`auditTasks`)
   - 如果有审核任务,拒绝删除
   - 提示: "该样品已有审核任务,无法删除"

3. **报告** (`reports`)
   - 如果已生成报告,拒绝删除
   - 提示: "该样品已生成报告,无法删除"

### 权限要求
删除操作需要 `sample:delete` 权限:
- 普通用户: 无权限
- 管理员: 有权限
- 实验室主管: 有权限

## 前端集成

前端已经实现了删除功能,现在可以正常工作:

### 单个删除
```javascript
// SampleManagement.vue
const handleDelete = async (row: Sample) => {
  await ElMessageBox.confirm(
    `确定要删除样品"${row.sampleName}"吗？此操作不可恢复。`,
    '删除确认',
    { type: 'warning' }
  )
  
  await sampleStore.deleteSample(row.id)
  ElMessage.success('删除成功')
  await fetchSamples()
}
```

### 批量删除
```javascript
const handleBatchDelete = async () => {
  await ElMessageBox.confirm(
    `确定要删除选中的 ${selectedSamples.value.length} 个样品吗？`,
    '批量删除确认',
    { type: 'warning' }
  )
  
  const ids = selectedSamples.value.map(s => s.id)
  await sampleStore.batchDelete(ids)
  ElMessage.success('批量删除成功')
  selectedSamples.value = []
  await fetchSamples()
}
```

## API文档

### 删除单个样品
```http
DELETE /api/samples/:id
Authorization: Bearer <token>

Response 200:
{
  "message": "样品删除成功"
}

Response 400:
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "该样品已有检测结果,无法删除"
  }
}

Response 404:
{
  "error": {
    "code": "NOT_FOUND",
    "message": "样品不存在"
  }
}
```

### 批量删除样品
```http
POST /api/samples/batch-delete
Authorization: Bearer <token>
Content-Type: application/json

{
  "ids": ["uuid1", "uuid2", "uuid3"]
}

Response 200:
{
  "message": "批量删除完成: 成功2个, 失败1个",
  "data": {
    "success": 2,
    "failed": 1,
    "errors": [
      "样品 uuid3: 该样品已有检测结果,无法删除"
    ]
  }
}
```

## 测试步骤

1. **重启后端服务**
   ```bash
   cd backend-api
   npm run dev
   ```

2. **运行测试脚本**
   ```bash
   node test-sample-delete.js
   ```

3. **前端测试**
   - 登录系统
   - 进入样品列表
   - 选择一个样品
   - 点击"更多" → "删除"
   - 确认删除
   - 验证样品从列表中消失

4. **批量删除测试**
   - 勾选多个样品
   - 点击"批量删除"按钮
   - 确认删除
   - 验证所有选中样品被删除

## 注意事项

1. **删除是软删除**
   - 样品状态变为 `ARCHIVED`
   - 数据仍在数据库中
   - 可以通过数据库直接恢复

2. **权限控制**
   - 确保用户有 `sample:delete` 权限
   - 管理员默认有此权限

3. **关联数据检查**
   - 只能删除没有关联数据的样品
   - 已有检测结果/审核/报告的样品无法删除
   - 这是为了保证数据完整性

4. **审计日志**
   - 所有删除操作都会记录日志
   - 包括操作人、时间、样品信息

## 后续优化建议

1. **恢复功能**
   - 添加恢复已删除样品的功能
   - 管理员可以查看和恢复ARCHIVED状态的样品

2. **级联删除选项**
   - 提供强制删除选项(删除关联数据)
   - 需要更高权限
   - 需要二次确认

3. **删除原因记录**
   - 记录删除原因
   - 便于审计追踪

4. **定期清理**
   - 定期清理长期ARCHIVED的样品
   - 释放存储空间
