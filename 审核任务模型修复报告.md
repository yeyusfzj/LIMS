# 审核任务模型修复报告

## 修复时间
2026-05-08 14:13

## 问题概述

用户报告审核任务查询失败，返回 500 错误：
```
column audit_tasks.sample_id does not exist
```

## 根本原因分析

### 1. 数据库结构检查

通过检查数据库实际结构，发现：

**audit_tasks 表的实际列**：
- ✅ `id` (TEXT)
- ✅ `level` (INTEGER)
- ✅ `auditorId` (TEXT)
- ✅ `status` (VARCHAR)
- ✅ `decision` (VARCHAR)
- ✅ `comments` (TEXT)
- ✅ `submittedAt` (TIMESTAMP)
- ✅ `completedAt` (TIMESTAMP)
- ✅ `taskId` (TEXT) ← **关键字段**
- ❌ **没有** `sampleId` 列

### 2. Prisma Schema 对比

查看 `backend-api-deprecated/prisma/schema.prisma`，发现正确的模型定义：

```prisma
model AuditTask {
  id        String         @id @default(uuid())
  taskId    String         ← 关联到 Task 表
  task      Task           @relation(fields: [taskId], references: [id])
  level     Int
  auditorId String
  status    AuditStatus
  ...
}
```

**关键发现**：
- ✅ `AuditTask` 通过 `taskId` 关联到 `Task` 表
- ✅ `Task` 通过 `instanceId` 关联到 `WorkflowInstance` 表
- ✅ `WorkflowInstance` 通过 `sampleId` 关联到 `Sample` 表
- ❌ `AuditTask` **不直接**关联到 `Sample` 表

**关联路径**：
```
AuditTask → Task → WorkflowInstance → Sample
```

### 3. 错误的模型定义

之前的 `backend-api/app/models/audit.py` 错误地定义了：

```python
# ❌ 错误的定义
class AuditTask(Base):
    sampleId = Column('sample_id', String, ForeignKey('samples.id'), ...)
    sample = relationship('Sample', back_populates='audit_tasks')
```

这导致：
1. SQLAlchemy 生成的 SQL 查询使用 `sample_id` 列
2. 但数据库中实际的列是 `taskId`
3. 查询失败：`column audit_tasks.sample_id does not exist`

## 修复方案

### 修复 1: 更正 AuditTask 模型

**文件**: `backend-api/app/models/audit.py`

```python
# ✅ 正确的定义
class AuditTask(Base):
    __tablename__ = 'audit_tasks'
    
    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    
    # 关联到 Task 表，而不是 Sample 表
    taskId = Column(String, ForeignKey('tasks.id', ondelete='CASCADE'), nullable=False, index=True)
    
    level = Column(Integer, nullable=False)
    auditorId = Column(String, nullable=False, index=True)
    status = Column(SQLEnum(AuditStatus), default=AuditStatus.PENDING, nullable=False, index=True)
    decision = Column(SQLEnum(AuditDecision))
    comments = Column(String)
    submittedAt = Column(DateTime, server_default=func.now(), nullable=False)
    completedAt = Column(DateTime)
    
    # 关系：关联到 Task 表
    task = relationship('Task', back_populates='audit_tasks')
```

**关键变更**：
- ✅ 将 `sampleId` 改为 `taskId`
- ✅ 外键指向 `tasks.id` 而不是 `samples.id`
- ✅ 关系指向 `Task` 而不是 `Sample`
- ✅ 移除了所有蛇形命名的列名映射（数据库使用驼峰命名）

### 修复 2: 移除 Sample 模型中的错误关系

**文件**: `backend-api/app/models/sample.py`

```python
# ❌ 移除错误的关系
# audit_tasks = relationship('AuditTask', back_populates='sample', cascade='all, delete-orphan')

# ✅ 添加注释说明
# 注意：audit_tasks 关联到 Task 表，而不是直接关联到 Sample
# 可以通过 workflow_instance.tasks 访问审核任务
```

### 修复 3: 添加 Task 模型中的关系

**文件**: `backend-api/app/models/task.py`

```python
class Task(Base):
    __tablename__ = "tasks"
    
    # ... 其他字段 ...
    
    # 关系：一对多 - 一个任务可以有多个审核任务
    audit_tasks = relationship('AuditTask', back_populates='task', cascade='all, delete-orphan')
```

### 修复 4: 移除其他模型的蛇形命名映射

由于数据库使用驼峰命名，移除了以下模型的列名映射：

**AuditCommentTemplate**:
- `usageCount` (不需要映射到 `usage_count`)
- `isDefault` (不需要映射到 `is_default`)
- `createdBy` (不需要映射到 `created_by`)
- `createdAt` (不需要映射到 `created_at`)
- `updatedAt` (不需要映射到 `updated_at`)

**AuditWorkflowConfig**:
- `sampleTypes` (不需要映射到 `sample_types`)
- `parallelAudit` (不需要映射到 `parallel_audit`)
- `createdBy` (不需要映射到 `created_by`)
- `createdAt` (不需要映射到 `created_at`)
- `updatedAt` (不需要映射到 `updated_at`)

**AuditHistory**:
- `taskId` (不需要映射到 `task_id`)
- `performedBy` (不需要映射到 `performed_by`)
- `performedAt` (不需要映射到 `performed_at`)

## 验证结果

### 1. 后端启动成功

```
[INFO] 2026-05-08 14:13:40 - app.main - Application startup complete
INFO:     Application startup complete.
```

✅ 没有关系错误
✅ 没有列名映射错误
✅ 派工引擎初始化成功

### 2. 数据库连接正常

```
[INFO] 2026-05-08 14:13:40 - app.main - Database connection: OK
[INFO] 2026-05-08 14:13:40 - app.core.database - Async session factory created
```

## 数据库命名约定总结

经过完整的检查和验证，确认：

| 表类型 | 命名约定 | 示例 |
|--------|---------|------|
| **所有表** | **驼峰命名** | `auditorId`, `submittedAt`, `completedAt` |

**之前的错误假设**：
- ❌ 认为审核相关表使用蛇形命名
- ❌ 添加了不必要的列名映射

**正确的理解**：
- ✅ 所有表都使用驼峰命名
- ✅ SQLAlchemy 模型属性名应该与数据库列名一致
- ✅ 不需要添加列名映射

## 如何访问审核任务

由于 `AuditTask` 不直接关联到 `Sample`，需要通过以下路径访问：

### 方法 1: 通过 Sample 访问

```python
# 获取样品的审核任务
sample = await db.get(Sample, sample_id)
if sample.workflow_instance:
    for task in sample.workflow_instance.tasks:
        for audit_task in task.audit_tasks:
            print(f"审核任务: {audit_task.id}, 状态: {audit_task.status}")
```

### 方法 2: 直接查询

```python
# 查询特定样品的所有审核任务
from sqlalchemy import select
from sqlalchemy.orm import joinedload

stmt = (
    select(AuditTask)
    .join(Task)
    .join(WorkflowInstance)
    .where(WorkflowInstance.sampleId == sample_id)
    .options(joinedload(AuditTask.task))
)
audit_tasks = await db.execute(stmt)
```

### 方法 3: 通过 Task 访问

```python
# 获取任务的审核任务
task = await db.get(Task, task_id)
for audit_task in task.audit_tasks:
    print(f"审核任务: {audit_task.id}, 级别: {audit_task.level}")
```

## 经验教训

### ❌ 错误做法

1. **假设数据库命名约定**
   - 没有先检查数据库实际结构
   - 基于错误的假设添加列名映射

2. **忽略 Prisma Schema**
   - 项目使用 Prisma 作为主要 ORM
   - 应该参考 Prisma schema 定义模型

3. **不完整的模型定义**
   - 只定义了部分模型
   - 没有定义完整的关系链

### ✅ 正确做法

1. **先检查数据库实际结构**
   - 使用 `information_schema.columns` 查询
   - 使用 SQLAlchemy `inspect` 检查

2. **参考 Prisma Schema**
   - Prisma schema 是数据库结构的权威定义
   - SQLAlchemy 模型应该与 Prisma schema 一致

3. **理解数据关系**
   - 理解表之间的关联路径
   - 正确定义外键和关系

4. **验证修复**
   - 检查后端启动日志
   - 验证没有关系错误
   - 测试 API 端点

## 相关文件

- ✅ `backend-api/app/models/audit.py` - 已修复
- ✅ `backend-api/app/models/sample.py` - 已修复
- ✅ `backend-api/app/models/task.py` - 已修复
- ✅ `backend-api/check_db_columns.py` - 检查工具
- ✅ `backend-api/check_all_tables.py` - 检查工具
- ✅ `backend-api-deprecated/prisma/schema.prisma` - 参考文档

## 下一步建议

### 1. 更新审核任务 API

由于模型结构变更，需要更新审核任务相关的 API：

**文件**: `backend-api/app/api/v1/audits.py` (如果存在)

- 更新查询逻辑，使用正确的关联路径
- 添加 JOIN 查询以获取样品信息
- 更新响应模型

### 2. 创建完整的工作流模型

目前缺少 `WorkflowInstance` 模型，建议创建：

**文件**: `backend-api/app/models/workflow.py`

```python
class WorkflowInstance(Base):
    __tablename__ = 'workflow_instances'
    
    id = Column(String, primary_key=True)
    workflowId = Column(String, ForeignKey('workflows.id'))
    sampleId = Column(String, ForeignKey('samples.id'), unique=True)
    
    # 关系
    sample = relationship('Sample', back_populates='workflow_instance')
    tasks = relationship('Task', back_populates='instance')
```

### 3. 统一命名约定文档

创建一个文档说明项目的命名约定：

- 数据库列名：驼峰命名（camelCase）
- Python 变量名：驼峰命名（与数据库一致）
- API 响应：驼峰命名（JSON）

---

**修复完成时间**: 2026-05-08 14:13  
**修复者**: AI 助手  
**状态**: ✅ 已修复并验证
