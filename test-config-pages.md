# 审核配置与判定规则配置页面对比测试

## 测试目的
验证审核配置和判定规则配置两个页面功能区分明确，内容不再重复。

## 页面功能对比

### 1. 审核配置页面 (AuditWorkflowConfig.vue)
**路径**: `/audit/workflow-config`
**功能**: 配置审核流程和审核级别（多级审核流程）

**核心字段**:
- 级别顺序 (order): 审核的先后顺序
- 级别名称 (name): 如"分析审核"、"样品审核"、"技术审核"
- 描述 (description): 审核级别的说明
- 审核角色 (role): 执行该级别审核的角色
- 是否必需 (required): 该审核级别是否必须完成

**示例数据**:
1. 分析审核 - 对检测结果进行初步审核，确保数据准确性
2. 样品审核 - 对样品信息和检测流程进行审核
3. 技术审核 - 对技术方法和结果判定进行最终审核

**使用场景**: 
- 定义检测报告的审核流程
- 设置多级审核机制
- 分配审核权限给不同角色

---

### 2. 判定规则配置页面 (JudgmentRuleConfig.vue)
**路径**: `/quality/judgment-rule-config`
**功能**: 配置质量判定标准和规则（检测结果的合格/不合格判定）

**核心字段**:
- 规则名称 (name): 如"pH值判定"、"铅含量判定"
- 检测参数 (parameter): 如"pH"、"铅(Pb)"、"COD"
- 判定标准 (standard): 如"GB 5749-2006 生活饮用水卫生标准"
- 比较运算符 (operator): <, ≤, =, ≥, >, 在范围内, 不在范围内
- 阈值 (threshold/minValue/maxValue): 判定的数值标准
- 判定结果 (result): 合格/不合格

**示例数据**:
1. pH值判定 - pH在6.5~8.5范围内为合格
2. 铅含量判定 - 铅(Pb) ≤ 0.01 mg/L为合格
3. COD判定 - COD ≤ 15 mg/L为合格
4. 农药残留判定 - 有机磷农药 < 0.05 mg/kg为合格

**使用场景**:
- 定义检测参数的合格标准
- 自动判定检测结果是否合格
- 基于国家标准或行业标准配置判定规则

---

## 功能区分总结

| 对比项 | 审核配置 | 判定规则配置 |
|--------|---------|-------------|
| **核心目的** | 配置审核流程 | 配置质量判定标准 |
| **关注点** | 谁来审核、审核顺序 | 什么是合格、判定标准 |
| **配置对象** | 审核级别和角色 | 检测参数和阈值 |
| **业务场景** | 多级审核流程管理 | 自动化质量判定 |
| **输出结果** | 审核通过/不通过 | 合格/不合格 |
| **依据** | 组织架构和权限 | 国家标准和技术规范 |

---

## 浏览器测试步骤

### 测试审核配置页面
1. 启动前端服务: `cd vue-project && npm run dev`
2. 访问: http://localhost:5173/audit/workflow-config
3. 验证页面标题: "审核级别配置"
4. 点击"添加审核级别"按钮
5. 检查表单字段:
   - ✓ 级别顺序
   - ✓ 级别名称
   - ✓ 描述
   - ✓ 审核角色
   - ✓ 是否必需
6. 查看示例数据是否为审核级别相关内容

### 测试判定规则配置页面
1. 访问: http://localhost:5173/quality/judgment-rule-config
2. 验证页面标题: "质量判定规则配置"
3. 点击"添加判定规则"按钮
4. 检查表单字段:
   - ✓ 规则名称
   - ✓ 检测参数
   - ✓ 判定标准
   - ✓ 比较运算符
   - ✓ 阈值/范围
   - ✓ 判定结果
5. 查看示例数据是否为质量判定规则相关内容

---

## 预期结果

✅ 两个页面功能完全不同
✅ 审核配置页面专注于审核流程管理
✅ 判定规则配置页面专注于质量标准判定
✅ 页面标题、字段、示例数据均有明确区分
✅ 无代码语法错误

---

## 测试状态

- [x] 代码修改完成
- [x] 语法检查通过
- [ ] 浏览器功能测试（需要用户手动测试）

## 备注

修改已完成，两个页面现在有明确的功能区分：
- **审核配置**: 管理多级审核流程和审核角色
- **判定规则配置**: 管理检测参数的质量判定标准

建议在浏览器中实际访问两个页面，验证功能是否符合预期。
