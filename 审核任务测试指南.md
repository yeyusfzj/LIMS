# 审核任务测试指南

## 测试时间
2026-05-04

## 问题修复
✅ 修复了任务类型判断函数定义顺序问题
✅ 将判断函数移到 computed 之前，确保正确执行

## 测试步骤

### 1. 访问审核任务列表
打开浏览器访问：http://localhost:5173/audit/tasks

### 2. 查看不同类型的审核任务

当前系统中有以下5种类型的审核任务：

#### ① 新产品配方研发 (RESEARCH)
- **级别**：样品审核
- **状态**：待审核
- **预期显示**：
  - 产品信息（产品名称、编号、类型、研发阶段）
  - 负责人和开始日期
  - 产品描述

#### ② 设备采购审批 (APPROVAL)
- **级别**：技术审核
- **状态**：待审核
- **预期显示**：
  - 审批信息（审批类型、申请人、申请时间）
  - 审批内容和金额（450,000 CNY）
  - 审批原因
  - 审批项目清单表格（高效液相色谱仪）

#### ③ 生产工艺优化 (PRODUCTION)
- **级别**：分析审核
- **状态**：审核中
- **预期显示**：
  - 生产信息（批次号、产品名称、生产数量）
  - 质量状态（合格）
  - 时间信息和进度条（75%）
  - 工艺优化项表格（混合时间、温度控制）

#### ④ 质量体系文件评审 (REVIEW)
- **级别**：质量审核
- **状态**：待审核
- **预期显示**：
  - 文档信息（标题、类型、版本、作者）
  - 页数（45页）
  - 主要变更内容列表（3项变更）
  - 评审人员表格（质量总监、技术总监、总经理）

#### ⑤ 仪器设备校准 (CALIBRATION) ⭐ 重点测试
- **级别**：分析审核
- **状态**：已通过
- **预期显示**：
  - 设备信息（电子天平、EQ-2023-056）
  - 校准信息（校准日期、下次校准日期、校准人员）
  - 校准结果（合格、精度±0.001g、证书编号）
  - **校准测试点表格**（3个测试点）：
    | 标准值 | 测量值 | 偏差 | 结果 |
    |--------|--------|------|------|
    | 0g | 0.000g | 0.000g | 通过 |
    | 100g | 100.001g | +0.001g | 通过 |
    | 200g | 199.999g | -0.001g | 通过 |

### 3. 检查控制台输出

打开浏览器开发者工具（F12），切换到 Console 标签，应该看到类似输出：

```
=== 任务数据调试 ===
任务类型 (nodeType): CALIBRATION
任务名称 (nodeName): 仪器设备校准
任务结果数据: {calibrationInfo: {...}, testPoints: [...]}
是否为校准任务: true
是否为生产任务: false
是否为评审任务: false
==================
```

### 4. 验证要点

#### ✅ 校准任务（最重要）
- [ ] 不再显示原始JSON代码
- [ ] 显示结构化的校准信息表格
- [ ] 显示校准测试点表格
- [ ] 测试点表格有4列：标准值、测量值、偏差、结果
- [ ] 结果列显示绿色"通过"标签

#### ✅ 生产任务
- [ ] 显示生产信息表格
- [ ] 显示进度条（75%）
- [ ] 显示工艺优化项表格

#### ✅ 评审任务
- [ ] 显示文档信息
- [ ] 显示变更内容列表（3项）
- [ ] 显示评审人员表格（3人）

#### ✅ 审批任务
- [ ] 显示审批信息
- [ ] 显示审批金额（450,000 CNY）
- [ ] 显示审批项目清单表格

#### ✅ 研发任务
- [ ] 显示产品信息
- [ ] 显示研发阶段
- [ ] 显示产品描述

## 常见问题排查

### 问题1：仍然显示JSON代码
**原因**：浏览器缓存
**解决**：
1. 按 Ctrl+Shift+R 强制刷新页面
2. 或清除浏览器缓存后重新访问

### 问题2：页面报错
**原因**：前端服务未正确重启
**解决**：
1. 检查前端服务是否运行：http://localhost:5173
2. 如果未运行，在 vue-project 目录执行：`npm run dev`

### 问题3：控制台没有调试输出
**原因**：未打开审核任务详情页面
**解决**：
1. 必须点击具体的审核任务进入详情页面
2. 列表页面不会有调试输出

## 技术说明

### 修复的核心问题
在 Vue 3 的 `<script setup>` 中，函数必须在使用前定义。原来的代码中：
- `taskRelatedData` computed 在第545行使用了判断函数
- 但判断函数定义在第760行
- 导致 computed 执行时函数未定义，条件判断失败

### 修复方案
将所有判断函数移到 `taskRelatedData` computed 之前定义，确保：
```typescript
// 1. 先定义判断函数
const isCalibrationTask = () => { ... }
const isProductionTask = () => { ... }
const isReviewTask = () => { ... }

// 2. 再定义使用这些函数的 computed
const taskRelatedData = computed(() => {
  // 可以正常调用上面定义的函数
  if (isCalibrationTask()) { ... }
})
```

## 预期结果

所有实验室相关任务类型都应该以结构化的表格和描述形式显示，不再显示原始JSON代码。特别是校准任务，应该清晰地显示设备信息、校准结果和测试点数据。

## 下一步

如果测试通过，可以：
1. 移除调试输出（console.log）
2. 根据实际需求调整显示字段
3. 添加更多任务类型的支持
4. 优化表格样式和布局
