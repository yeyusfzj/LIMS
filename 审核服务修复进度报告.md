# 审核服务修复进度报告

## 当前状态

### ✅ 已完成

1. **模型定义修复**
   - ✅ 创建了 `WorkflowInstance` 模型
   - ✅ 创建了 `Workflow` 模型
   - ✅ 更新了 `Sample` 模型添加 `workflow_instance` 关系
   - ✅ `Task` 模型已有正确的关系定义
   - ✅ 后端成功启动，没有模型错误

2. **文件备份**
   - ✅ 备份了原始的 `audit_service.py` 文件

### ⚠️ 待修复

审核服务 (`audit_service.py`) 中需要修复的方法：

#### 1. `submit_for_audit` 方法（第 42-145 行）

**问题**：
- 使用 `AuditTask.sampleId` 创建审核任务
- 需要先创建或获取 `Task` 和 `WorkflowInstance`

**修复方案**：
```python
async def submit_for_audit(self, db: AsyncSession, dto: SubmitAuditDto):
    # 1. 验证样品存在
    # 2. 获取或创建 WorkflowInstance
    # 3. 为每个审核级别创建 Task
    # 4. 为每个 Task 创建 AuditTask
```

#### 2. `perform_audit` 方法（第 147-318 行）

**问题**：
- 使用 `AuditTask.sampleId` 查询相关审核任务
- 需要通过 `Task` → `WorkflowInstance` → `Sample` 获取样品信息

**修复方案**：
```python
# 获取样品 ID
task_obj = await db.get(Task, audit_task.taskId)
instance = await db.get(WorkflowInstance, task_obj.instanceId)
sample_id = instance.sampleId
```

#### 3. `_handle_audit_return` 方法（第 320-343 行）

**问题**：
- 使用 `AuditTask.sampleId` 查询审核任务

**修复方案**：
- 通过 JOIN 查询获取相关审核任务

#### 4. `list_audit_tasks` 方法（第 393-437 行）

**问题**：
- 使用 `AuditTask.sampleId` 作为查询条件

**修复方案**：
```python
if query.sampleId:
    # 需要 JOIN Task 和 WorkflowInstance
    stmt = (
        stmt
        .join(Task, AuditTask.taskId == Task.id)
        .join(WorkflowInstance, Task.instanceId == WorkflowInstance.id)
        .where(WorkflowInstance.sampleId == query.sampleId)
    )
```

#### 5. `_format_audit_task` 方法（第 467-577 行）

**问题**：
- 使用 `AuditTask.sampleId` 获取样品信息
- 返回的 `AuditTaskResponse` 包含 `sampleId` 字段

**修复方案**：
```python
# 通过 Task → WorkflowInstance → Sample 获取样品信息
task_obj = await db.get(Task, audit_task.taskId)
if task_obj and task_obj.instanceId:
    instance = await db.get(WorkflowInstance, task_obj.instanceId)
    if instance and instance.sampleId:
        sample = await db.get(Sample, instance.sampleId)
```

#### 6. `validate_release_conditions` 方法（第 1145-1189 行）

**问题**：
- 使用 `AuditTask.sampleId` 查询审核任务

**修复方案**：
- 通过 JOIN 查询获取相关审核任务

#### 7. `export_audit_tasks` 方法（第 1247-1293 行）

**问题**：
- 使用 `AuditTask.sampleId` 作为查询条件

**修复方案**：
- 与 `list_audit_tasks` 相同的修复方案

## 修复复杂度分析

### 高复杂度方法

1. **`submit_for_audit`** - 需要创建完整的工作流实例和任务
2. **`perform_audit`** - 需要处理多级审核逻辑
3. **`_format_audit_task`** - 需要重写样品信息获取逻辑

### 中等复杂度方法

1. **`list_audit_tasks`** - 需要添加 JOIN 查询
2. **`validate_release_conditions`** - 需要添加 JOIN 查询
3. **`export_audit_tasks`** - 需要添加 JOIN 查询

### 低复杂度方法

1. **`_handle_audit_return`** - 简单的查询修改

## 估计工作量

- **完整修复**: 3-4 小时
- **核心功能修复**（查询和格式化）: 1-2 小时
- **创建功能修复**（提交审核）: 1-2 小时

## 建议的修复顺序

### 阶段 1: 核心查询功能（优先级：高）

1. ✅ 创建 `WorkflowInstance` 模型
2. ⏳ 修复 `list_audit_tasks` - 让前端能够查询审核任务
3. ⏳ 修复 `_format_audit_task` - 正确显示样品信息
4. ⏳ 修复 `get_audit_task` - 获取单个审核任务详情

**预期结果**: 前端能够查看审核任务列表和详情

### 阶段 2: 审核执行功能（优先级：中）

1. ⏳ 修复 `perform_audit` - 执行审核决策
2. ⏳ 修复 `_handle_audit_return` - 处理审核退回
3. ⏳ 修复 `reassign_audit_task` - 审核任务转交

**预期结果**: 用户能够执行审核操作

### 阶段 3: 审核创建功能（优先级：中）

1. ⏳ 修复 `submit_for_audit` - 提交样品审核
2. ⏳ 创建辅助方法 `_get_or_create_workflow_instance`
3. ⏳ 创建辅助方法 `_create_audit_task_for_level`

**预期结果**: 用户能够提交样品审核

### 阶段 4: 其他功能（优先级：低）

1. ⏳ 修复 `validate_release_conditions` - 样品放行验证
2. ⏳ 修复 `export_audit_tasks` - 导出审核数据
3. ⏳ 修复 `get_audit_statistics` - 审核统计

**预期结果**: 完整的审核功能

## 当前决策点

由于修复工作量较大，建议采用分阶段修复策略：

### 选项 1: 立即完成阶段 1（推荐）

**工作内容**:
- 修复 `list_audit_tasks` 方法
- 修复 `_format_audit_task` 方法
- 修复 `get_audit_task` 方法

**时间**: 30-45 分钟  
**收益**: 前端能够查看审核任务列表

### 选项 2: 完成所有阶段

**工作内容**:
- 修复所有 7 个方法
- 创建辅助方法
- 全面测试

**时间**: 3-4 小时  
**收益**: 完整的审核功能

### 选项 3: 暂停修复，等待用户反馈

**原因**:
- 修复工作量大
- 需要确认业务需求
- 可能需要调整数据库结构

## 技术债务说明

当前的审核服务设计假设 `AuditTask` 直接关联到 `Sample`，但实际的数据库结构是：

```
AuditTask → Task → WorkflowInstance → Sample
```

这种设计的优点：
- ✅ 符合工作流设计
- ✅ 支持复杂的审核流程
- ✅ 可扩展性好

这种设计的缺点：
- ❌ 查询复杂度增加（需要多次 JOIN）
- ❌ 代码修改量大
- ❌ 性能可能受影响

## 替代方案

### 方案 A: 添加冗余字段（不推荐）

在 `audit_tasks` 表中添加 `sampleId` 列作为冗余字段：

**优点**:
- 查询简单
- 代码修改量小

**缺点**:
- 数据冗余
- 可能导致数据不一致
- 违反数据库设计原则

### 方案 B: 创建数据库视图（可考虑）

创建一个数据库视图，将 `AuditTask`、`Task`、`WorkflowInstance`、`Sample` 关联起来：

```sql
CREATE VIEW audit_task_with_sample AS
SELECT 
    at.*,
    wi.sampleId,
    s.barcode,
    s.sampleNumber
FROM audit_tasks at
JOIN tasks t ON at.taskId = t.id
JOIN workflow_instances wi ON t.instanceId = wi.id
JOIN samples s ON wi.sampleId = s.id;
```

**优点**:
- 查询简单
- 不破坏数据库结构
- 性能较好

**缺点**:
- 需要创建视图
- SQLAlchemy 需要额外配置

### 方案 C: 使用 SQLAlchemy 关系加载（推荐）

使用 SQLAlchemy 的 `joinedload` 或 `selectinload` 预加载关联数据：

```python
stmt = (
    select(AuditTask)
    .options(
        joinedload(AuditTask.task)
        .joinedload(Task.instance)
        .joinedload(WorkflowInstance.sample)
    )
)
```

**优点**:
- 符合 ORM 设计
- 代码清晰
- 可维护性好

**缺点**:
- 需要修改代码
- 查询可能较慢

## 下一步行动

请确认您希望采用哪种方案：

1. **继续阶段 1 修复**（推荐）- 让查询功能可用
2. **完整修复所有功能** - 需要 3-4 小时
3. **采用替代方案 B**（数据库视图）- 简化查询
4. **暂停并重新评估** - 讨论业务需求

---

**报告时间**: 2026-05-08 14:25  
**状态**: 等待用户确认下一步行动
