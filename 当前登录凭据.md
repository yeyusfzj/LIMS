# 当前登录凭据 ✅ 已确认可用

## FastAPI 后端 (当前使用)

**后端地址**: http://localhost:8000  
**前端地址**: http://localhost:5173

### 管理员账号 ✅
- **用户名**: `admin`
- **密码**: `admin123`
- **角色**: admin
- **状态**: ✅ 已验证可登录 (2026-04-28)

### 测试用户账号
- **用户名**: `testuser`
- **密码**: `admin123` (通常与管理员相同)
- **角色**: user, lab_technician

---

## 重要说明

### 两个后端的密码不同

1. **FastAPI 后端** (端口 8000) - **当前正在使用** ✅
   - 密码: `admin123`
   
2. **Node.js 后端** (端口 3000) - **未使用**
   - 密码: `Admin@123456`

### 如果密码不对

如果 `admin123` 无法登录,可以运行以下脚本重置密码:

```bash
cd fastapi-backend
venv\Scripts\activate
python reset_admin_password.py
```

或者使用简单密码设置脚本:

```bash
cd fastapi-backend
venv\Scripts\activate
python set_simple_password.py
```

---

## 快速测试登录

### 使用 curl 测试:

```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 预期响应:

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "accessToken": "eyJ...",
    "user": {
      "id": "...",
      "username": "admin",
      "roles": ["admin"]
    }
  }
}
```
