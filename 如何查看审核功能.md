# 如何查看审核功能

## 问题说明

你提到"看不到审核功能"。让我帮你定位问题。

## 检查清单

### 1. 确认服务已启动

**后端服务** (端口 3000):
- 正在启动中...
- 启动完成后访问: http://localhost:3000/health

**前端服务** (端口 5173):
- 正在启动中...
- 启动完成后访问: http://localhost:5173/

### 2. 审核功能的位置

审核功能在系统中有多个入口：

#### 方式一：通过左侧菜单访问

1. 登录系统 (admin / Admin@123456)
2. 在左侧菜单栏找到 **"审核管理"** 菜单（第4个菜单项）
3. 展开后可以看到：
   - 审核任务
   - 质量判定
   - 样品放行

#### 方式二：直接访问URL

如果菜单不显示，可以直接在浏览器地址栏输入：

```
http://localhost:5173/audit/tasks
```

### 3. 可能的原因和解决方案

#### 原因1：浏览器缓存
**解决方案**: 强制刷新
- Windows: `Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac: `Cmd + Shift + R`

#### 原因2：前端服务未完全启动
**解决方案**: 等待服务完全启动
- 查看终端输出，等待显示 "ready" 或 "Local: http://localhost:5173/"
- 通常需要 10-30 秒

#### 原因3：路由配置问题
**解决方案**: 检查浏览器控制台
1. 按 F12 打开开发者工具
2. 查看 Console 标签是否有错误
3. 查看 Network 标签，看 API 请求是否成功

#### 原因4：权限问题
**解决方案**: 确认使用管理员账号登录
- 用户名: `admin`
- 密码: `Admin@123456`

### 4. 验证配置

我已经检查过配置，确认：

✅ 路由已配置:
- `/audit/tasks` - 审核任务列表
- `/audit/task/:id` - 审核任务详情
- `/quality/judgment` - 质量判定

✅ 菜单已配置:
- 审核管理 > 审核任务
- 审核管理 > 质量判定
- 审核管理 > 样品放行

✅ 页面组件已存在:
- AuditTaskList.vue
- AuditTaskDetail.vue
- QualityJudgment.vue

✅ 后端API已实现并测试通过

### 5. 测试步骤

等待服务完全启动后（约1-2分钟），按以下步骤测试：

**步骤1**: 访问前端
```
http://localhost:5173/
```

**步骤2**: 登录
- 用户名: admin
- 密码: Admin@123456

**步骤3**: 查看菜单
- 左侧菜单栏应该显示"审核管理"菜单项
- 如果看不到，尝试刷新页面

**步骤4**: 直接访问
如果菜单不显示，直接访问：
```
http://localhost:5173/audit/tasks
```

**步骤5**: 检查控制台
如果页面无法访问，按F12查看控制台错误信息

### 6. 当前系统状态

根据我的检查：

- ✅ 后端API已完全实现
- ✅ 前端路由已配置
- ✅ 前端菜单已配置
- ✅ 前端页面组件已存在
- ✅ 后端API测试通过（已成功执行审核操作）

**系统中已有的测试数据**:
- 10 个审核意见模板
- 6 个测试样品
- 1 个已完成的审核任务

### 7. 下一步

请告诉我：

1. **你能看到左侧菜单栏吗？**
   - 如果能，列出你看到的菜单项
   - 如果不能，可能是页面加载问题

2. **直接访问 http://localhost:5173/audit/tasks 能打开吗？**
   - 如果能打开，说明路由正常，只是菜单显示问题
   - 如果不能打开，请告诉我看到什么错误

3. **浏览器控制台有错误吗？**
   - 按 F12 打开开发者工具
   - 查看 Console 标签
   - 截图或复制错误信息

4. **你使用的是什么浏览器？**
   - Chrome / Firefox / Edge / Safari

## 快速诊断命令

运行以下命令检查配置：
```bash
node check-frontend-audit-menu.js
```

这会显示所有审核功能的配置状态。

## 需要更多帮助

如果以上方法都不行，请提供：
1. 浏览器截图（显示左侧菜单栏）
2. 控制台错误信息
3. 直接访问 /audit/tasks 的结果
