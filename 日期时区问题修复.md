# 日期时区问题修复

**修复时间**: 2026年4月27日 15:06  
**问题**: 创建样品时出现时区相关错误

## 问题描述

创建样品时返回400错误：

```
invalid input for query argument $11: datetime.datetime(2026, 4, 18, 0, 0, tzinfo=TzInfo(0))
(can't subtract offset-naive and offset-aware datetimes)
```

## 根本原因

前端使用 `new Date(formData.receivedDate)` 创建了**带时区**的Date对象，但数据库字段定义为 `TIMESTAMP WITHOUT TIME ZONE`（不带时区）。

**前端发送**:
```javascript
receivedDate: new Date('2026-04-18')  // 带时区: datetime.datetime(2026, 4, 18, 0, 0, tzinfo=TzInfo(0))
```

**数据库期望**:
```sql
TIMESTAMP WITHOUT TIME ZONE  -- 不带时区
```

## 解决方案

将日期转换为 `YYYY-MM-DD` 格式的字符串，而不是Date对象。

### 修改文件

`vue-project/src/views/sample/SampleRegistration.vue`

### 修改内容

添加日期格式化函数：

```typescript
// 将日期转换为 YYYY-MM-DD 格式的字符串
const formatDate = (date: string | Date) => {
  if (!date) return undefined
  const d = typeof date === 'string' ? new Date(date) : date
  return d.toISOString().split('T')[0]
}
```

使用格式化函数：

```typescript
// 创建模式
receivedDate: formatDate(formData.receivedDate),
samplingDate: formatDate(formData.receivedDate),

// 编辑模式
receivedDate: formatDate(formData.receivedDate),
```

## 修复位置

1. **创建样品** - 第347行
2. **编辑样品** - 第307行

## 测试验证

修复后，日期将以字符串格式发送：

```
receivedDate: "2026-04-18"  // 字符串格式，无时区信息
```

后端会正确解析为不带时区的时间戳。

---

**修复状态**: ✅ 已完成  
**影响范围**: 样品创建和编辑功能
