# 审核执行页面测试指南

## 测试步骤

### 1. 确认服务运行状态
- 后端服务：http://localhost:3000
- 前端服务：http://localhost:5173

### 2. 登录系统
访问：http://localhost:5173/#/login
- 用户名：admin
- 密码：Admin@123456

### 3. 访问审核任务列表
访问：http://localhost:5173/#/audit/tasks

### 4. 点击"执行"按钮
在待审核任务列表中，找到状态为"待审核"的任务，点击"执行"按钮

### 5. 检查审核执行页面
应该跳转到：http://localhost:5173/#/audit/execute?taskId=xxx

**预期显示内容：**
- 审核任务信息（任务编号、审核级别、审核状态、优先级等）
- 样品信息（样品条码、样品名称、样品类型、客户名称等）
- 检测结果表格（可能为空）
- 审核意见表单（包含通过、退回、要求补充三个按钮）

**如果页面显示空白，请检查：**
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页是否有错误
3. 查看 Network 标签页，检查 API 请求是否成功
4. 特别关注 `/api/audits/{taskId}` 请求的响应

## 已知问题和修复

### 问题1：执行按钮不显示
**原因**：后端返回的状态是大写 `PENDING`，前端判断条件是小写 `pending`

**修复**：在 `auditService.ts` 中添加了状态转换逻辑
```typescript
status: task.status?.toLowerCase() || 'pending'
```

### 问题2：资源不存在错误
**原因**：审核执行页面调用了不存在的 API `/audits/${taskId}/test-results`

**修复**：修改为从审核任务详情的 `sample.results` 字段获取检测结果

### 问题3：页面显示空白（当前问题）
**可能原因**：
1. 路由参数传递问题
2. API 数据加载失败
3. 条件渲染逻辑问题
4. 组件依赖缺失

## 调试信息

### 后端API测试结果
```bash
# 获取审核任务详情
GET /api/audits/7f9fd5ec-0418-46e3-8649-2b9f1b4610f0

# 响应数据
{
  "message": "查询成功",
  "data": {
    "id": "7f9fd5ec-0418-46e3-8649-2b9f1b4610f0",
    "status": "PENDING",
    "level": 3,
    "auditorId": "UNASSIGNED",
    "sample": {
      "barcode": "SP20260403000006",
      "sampleName": "测试水样-审核流程",
      "sampleType": "水质",
      "clientName": "测试客户",
      "priority": "NORMAL",
      "results": []  // 空数组
    }
  }
}
```

### 前端数据转换
auditService.ts 会将后端数据转换为：
```typescript
{
  id: "7f9fd5ec-0418-46e3-8649-2b9f1b4610f0",
  status: "pending",  // 转换为小写
  level: 3,
  levelName: "技术审核",  // 添加级别名称
  auditor: "UNASSIGNED",
  sampleName: "测试水样-审核流程",
  sampleBarcode: "SP20260403000006",
  priority: "normal",  // 转换为小写
  sample: { ... }
}
```

## 下一步调试

请在浏览器中执行以下操作：

1. 访问审核任务列表页面
2. 打开浏览器开发者工具（F12）
3. 切换到 Console 标签页
4. 点击任意待审核任务的"执行"按钮
5. 观察：
   - Console 中是否有错误信息
   - Network 中 API 请求是否成功
   - 页面是否显示内容或保持空白

请将观察到的错误信息反馈给我，我会根据具体错误进行修复。
