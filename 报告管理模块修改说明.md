# 报告管理模块修改说明

## 修改概述

根据审核管理的任务列表结构，对报告管理模块进行了重新设计和功能增强。主要修改包括：

1. **添加报告类型分类**（参考审核任务级别）
2. **创建新的报告模板**
3. **修改前端页面为网页端显示**（移除下载功能，保留打印功能）

---

## 一、后端修改

### 1.1 报告模型修改

**文件**: `fastapi-backend/app/models/report.py`

#### 新增报告类型枚举

```python
class ReportType(str, enum.Enum):
    """Report type enumeration - 参考审核任务级别"""
    ANALYSIS_REPORT = "ANALYSIS_REPORT"  # 分析报告
    SAMPLE_REPORT = "SAMPLE_REPORT"  # 样品报告
    TECHNICAL_REPORT = "TECHNICAL_REPORT"  # 技术报告
    QUALITY_REPORT = "QUALITY_REPORT"  # 质量报告
    COMPREHENSIVE_REPORT = "COMPREHENSIVE_REPORT"  # 综合报告
```

#### Report模型新增字段

```python
# Report type - 参考审核任务级别
reportType = Column(
    SQLEnum(ReportType, name="ReportType"),
    default=ReportType.ANALYSIS_REPORT,
    nullable=False,
    index=True
)
```

### 1.2 报告模板脚本

**文件**: `fastapi-backend/add-report-type-templates.py`

创建了5种报告类型的模板：

1. **分析报告模板** (`ANALYSIS_REPORT`)
   - 用于生成分析级别的检测报告
   - 包含详细的检测数据和分析结果
   - 适用于需要深入分析的检测项目

2. **样品报告模板** (`SAMPLE_REPORT`)
   - 用于生成样品级别的检测报告
   - 侧重样品信息和基础检测数据
   - 适用于常规样品检测

3. **技术报告模板** (`TECHNICAL_REPORT`)
   - 用于生成技术级别的检测报告
   - 包含技术分析和专业评估
   - 适用于技术审核和评估

4. **质量报告模板** (`QUALITY_REPORT`)
   - 用于生成质量级别的检测报告
   - 包含质量评估和合格判定
   - 适用于质量管理和认证

5. **综合报告模板** (`COMPREHENSIVE_REPORT`)
   - 用于生成综合性检测报告
   - 包含完整的检测信息、分析和评估
   - 适用于正式对外发布的报告

#### 运行脚本

```bash
cd fastapi-backend
python add-report-type-templates.py
```

---

## 二、前端修改

### 2.1 新增报告列表页面

**文件**: `vue-project/src/views/report/ReportList.vue`

#### 主要功能

1. **报告类型筛选**
   - 分析报告
   - 样品报告
   - 技术报告
   - 质量报告
   - 综合报告

2. **报告状态筛选**
   - 草稿
   - 待签名
   - 已签名
   - 已分发
   - 已撤回

3. **统计信息展示**
   - 总报告数
   - 待签名数量
   - 已签名数量
   - 已分发数量

4. **报告操作**
   - 查看报告（网页端显示）
   - 编辑报告（仅草稿状态）
   - 删除报告（仅草稿状态）
   - 打印报告（已签名或已分发状态）

5. **网页端显示**
   - 报告内容在对话框中以HTML格式显示
   - 支持打印功能
   - **移除了下载功能**（按需求）

### 2.2 路由配置修改

**文件**: `vue-project/src/router/index.ts`

新增路由：

```typescript
{
  path: 'report/list',
  name: 'report-list',
  component: () => import('@/views/report/ReportList.vue'),
  meta: { title: '报告列表' }
}
```

---

## 三、报告类型与审核任务级别对应关系

| 报告类型 | 对应审核级别 | 说明 |
|---------|------------|------|
| 分析报告 | 分析审核（Level 1） | 初级分析人员完成的检测报告 |
| 样品报告 | 样品审核（Level 2） | 样品管理人员审核的报告 |
| 技术报告 | 技术审核（Level 3） | 技术负责人审核的报告 |
| 质量报告 | 质量审核（Level 4） | 质量管理人员审核的报告 |
| 综合报告 | 全流程审核 | 经过所有级别审核的最终报告 |

---

## 四、数据库迁移

### 4.1 需要执行的SQL（如果使用Prisma）

```sql
-- 添加报告类型枚举
CREATE TYPE "ReportType" AS ENUM (
  'ANALYSIS_REPORT',
  'SAMPLE_REPORT',
  'TECHNICAL_REPORT',
  'QUALITY_REPORT',
  'COMPREHENSIVE_REPORT'
);

-- 在reports表中添加reportType字段
ALTER TABLE "reports" 
ADD COLUMN "reportType" "ReportType" NOT NULL DEFAULT 'ANALYSIS_REPORT';

-- 创建索引
CREATE INDEX "reports_reportType_idx" ON "reports"("reportType");
```

### 4.2 Prisma Schema修改

如果使用Prisma，需要在schema.prisma中添加：

```prisma
enum ReportType {
  ANALYSIS_REPORT
  SAMPLE_REPORT
  TECHNICAL_REPORT
  QUALITY_REPORT
  COMPREHENSIVE_REPORT
}

model Report {
  id            String       @id @default(uuid())
  reportNumber  String       @unique
  sampleId      String
  templateId    String
  reportType    ReportType   @default(ANALYSIS_REPORT)  // 新增字段
  content       String       @db.Text
  status        ReportStatus @default(DRAFT)
  version       Int          @default(1)
  generatedBy   String
  generatedAt   DateTime     @default(now())
  approvedAt    DateTime?
  recalledAt    DateTime?
  recallReason  String?
  
  sample        Sample       @relation(fields: [sampleId], references: [id])
  template      ReportTemplate @relation(fields: [templateId], references: [id])
  signatures    Signature[]
  distributions Distribution[]
  
  @@index([reportType])
  @@index([status])
  @@index([sampleId])
}
```

---

## 五、使用说明

### 5.1 生成报告

1. 进入"报告生成"页面
2. 选择样品
3. 选择对应类型的报告模板
4. 生成报告

### 5.2 查看报告

1. 进入"报告列表"页面
2. 使用筛选条件查找报告
3. 点击"查看"按钮
4. 报告内容在对话框中以网页形式显示

### 5.3 打印报告

1. 在报告查看对话框中
2. 点击"打印"按钮
3. 使用浏览器打印功能打印报告

---

## 六、注意事项

1. **报告类型字段是必填的**，默认值为`ANALYSIS_REPORT`
2. **已移除下载功能**，仅保留网页端显示和打印功能
3. **报告模板需要先添加到数据库**，运行`add-report-type-templates.py`脚本
4. **数据库迁移**需要根据实际使用的ORM工具执行相应的迁移命令
5. **前端路由**已更新，确保菜单配置中包含"报告列表"入口

---

## 七、后续优化建议

1. **报告模板管理**
   - 支持在线编辑报告模板
   - 支持模板版本管理
   - 支持模板预览功能

2. **报告审批流程**
   - 集成审核管理模块
   - 实现报告审批工作流
   - 支持多级审批

3. **报告统计分析**
   - 按报告类型统计
   - 按时间段统计
   - 生成报告分析图表

4. **报告导出功能**（可选）
   - 如果后续需要，可以添加PDF导出功能
   - 支持批量导出
   - 支持自定义导出格式

---

## 八、测试清单

- [ ] 后端模型修改验证
- [ ] 报告模板脚本执行
- [ ] 前端报告列表页面显示
- [ ] 报告类型筛选功能
- [ ] 报告状态筛选功能
- [ ] 报告查看功能（网页端显示）
- [ ] 报告打印功能
- [ ] 报告编辑功能（草稿状态）
- [ ] 报告删除功能（草稿状态）
- [ ] 统计信息准确性

---

## 九、相关文件清单

### 后端文件
- `fastapi-backend/app/models/report.py` - 报告模型（已修改）
- `fastapi-backend/add-report-type-templates.py` - 报告模板添加脚本（新增）

### 前端文件
- `vue-project/src/views/report/ReportList.vue` - 报告列表页面（新增）
- `vue-project/src/router/index.ts` - 路由配置（已修改）

### 文档文件
- `报告管理模块修改说明.md` - 本文档（新增）

---

## 十、联系与支持

如有问题或需要进一步的功能定制，请联系开发团队。
